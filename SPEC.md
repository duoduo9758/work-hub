# SPEC.md — work-hub 機能仕様と受け入れ条件

> 本ドキュメントは Step 1 の成果物。
> CHARTER.md v0.5 で確定したスコープに対し、**実装可能なレベルの受け入れ条件**を機能ごとに記述する。
> 作成日：2026-05-28 / バージョン：**v0.8**（ChatGPT休暇レビュー反映：h表記残骸全削除・5日警告名改名・法令免責・UI分離・adjustment配置/プレビュー・残量超過導線）
>
> ※ 「やらないこと」は CHARTER.md 第9章を正とする。本書では再掲しない（重複を避けるため）。
> ※ 段階リリース（v1/v2a〜e/v3/v4/v5）の順序や依存は Step 4 PLAN.md で扱う。本書は機能仕様。

---

## 目次

1. [全体構成](#1-全体構成)
2. [画面一覧](#2-画面一覧)
3. [主要ユーザーフロー](#3-主要ユーザーフロー)
4. [機能①：アクセスコード認証](#4-機能アクセスコード認証)
5. [機能②：日数進捗トラッカー](#5-機能日数進捗トラッカー)
6. [機能③：Todo（チェック付きアウトラインメモ）](#6-機能todoチェック付きアウトラインメモ)
7. [機能④：休暇管理](#7-機能休暇管理)
8. [機能⑤：タイマー](#8-機能タイマー)
9. [共通仕様](#9-共通仕様)

---

## 1. 全体構成

### 1-1. アプリの構造
- **シングルページアプリ（SPA）**：Vite + React 18 + .jsx
- **ルーティング**：簡易（タブ切替で画面切替、URL は変えない）
- **画面の大枠**：
  - **常時表示のヘッダー**（ピン留めトラッカーの進捗バー）
  - **本文エリア**（タブごとに切替）
  - **ボトムナビゲーション**（スマホ）/ **サイドナビ**（PC）：トラッカー / Todo / 休暇 / タイマー

### 1-2. データの所有者
- 1ユーザー = 1アクセスコード
- Firestore は `users/{accessCode}/...` のサブコレクションでデータ分離

### 1-3. 機密データ前提
- 本アプリには **社外秘・機密情報・顧客名・案件実名を入力しない前提** で運用する（ユーザー本人の自己管理）
- 画面上の注意書き表示はしない（ユーザー要望により撤廃）

---

## 2. 画面一覧

| 画面ID | 画面名 | 用途 | 機能 |
|---|---|---|---|
| S0 | ログイン | アクセスコード入力 | 認証 |
| S1 | トラッカー一覧 | 日数進捗トラッカーの一覧・追加・編集・削除・ピン留め | トラッカー |
| S2 | Todo メイン | プロジェクトサイドバー＋エディタエリア | Todo |
| S3 | 休暇管理 | 残量サマリ＋履歴一覧＋レコード追加/編集 | 休暇 |
| S4 | タイマー | カウントアップ／カウントダウン | タイマー |
| H | グローバルヘッダー | ピン留めトラッカーの進捗バー（S1〜S4 共通） | トラッカー |

---

## 3. 主要ユーザーフロー

### 3-1. 初回ログイン
```
S0 アクセスコード入力
 → アプリが存在チェック
   - 該当データなし → 新規ユーザーとしてデータ作成 → S1 へ
   - 該当データあり → 既存データ読込 → S1 へ
```

### 3-2. 日常の主要フロー（朝アプリを開いた時）
```
S0 自動ログイン（localStorage の前回コード）
 → S1 / S2 / S3 / S4 のいずれかを表示
 → ヘッダーにピン留めトラッカーが見える
 → タブ切替で各機能を行き来
```

### 3-3. プロジェクト切替フロー（Todo）
```
S2 で左サイドバー（PC）/ 上部タブ（スマホ）からプロジェクトA選択
 → S2 にプロジェクトA のメモ表示
 → 編集（自動保存）
 → プロジェクトB に切替
   - 切替前に強制保存
   - プロジェクトB のメモ表示
```

### 3-4. ピン留め変更フロー（トラッカー）
```
S1 でトラッカー一覧表示
 → 任意のトラッカーの「ピン留め」アイコンをタップ
 → ヘッダー H の表示が切り替わる
 → 別画面に行ってもヘッダーは追従
```

---

## 4. 機能①：アクセスコード認証

### 4-1. 機能概要
カスタム文字列ID（アクセスコード）でユーザーを識別する簡易認証。Firebase Auth は使用しない。

### 4-2. 画面（S0：ログイン）
- アプリタイトル「work-hub」
- アクセスコード入力欄
- 「ログイン」ボタン
- 形式の説明：英数字・アンダースコア・ハイフン、**8〜64文字**（推奨 12文字以上）

### 4-3. 受け入れ条件

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-AUTH-01 | 有効なアクセスコード（**8〜64文字、英数字・`_`・`-`**）を入力してログインボタンを押す | S1 トラッカー一覧画面が表示される |
| AC-AUTH-02 | 7文字以下のアクセスコードを入力してログインを押す | エラー：「**8文字以上で入力してください**」、ボタンが押せないか押しても進まない |
| AC-AUTH-03 | 65文字以上のアクセスコードを入力 | エラー：「64文字以内で入力してください」 |
| AC-AUTH-04 | 不正文字（スペース、日本語、記号 `! @ # $ %` 等）を含むコードを入力 | エラー：「英数字、アンダースコア、ハイフンのみ使用可能です」 |
| AC-AUTH-05 | 空文字でログインを押す | ボタンが非活性、押せない |
| AC-AUTH-06 | 初めて使うコードでログイン | 新規ユーザーとしてデータ作成、各機能の初期状態（空の一覧）が表示される |
| AC-AUTH-07 | 過去に使ったコードでログイン | 前回のデータが復元される |
| AC-AUTH-08 | ログイン成功時にアクセスコードを localStorage に保存 | ブラウザを閉じて再度開いても自動ログイン |
| AC-AUTH-09 | ログアウト操作（メニューから） | localStorage の認証情報を削除し、S0 に戻る |
| AC-AUTH-10 | ログイン時 Firestore 読み込み失敗 | エラー表示し、**S1 に遷移しない**（空データで新規ユーザー扱いせず、別端末データを上書きしない）。「再試行」ボタンで再読み込み |
| AC-AUTH-11 | ログアウト操作 | localStorage の認証情報をクリア。**タイマー実行中フラグ・音設定もクリア**（前ユーザーの設定が残らない） |
| AC-AUTH-12 | 別アクセスコードでログインし直し | 前ユーザーのタイマー localStorage はクリア。設定（音 ON/OFF・デフォルト時間）は新ユーザーのデフォルト値に戻る |
| AC-AUTH-13 | Todoに未保存変更がある状態でログアウト | 「未保存の変更があります。保存してログアウト / 破棄してログアウト / キャンセル」を表示。保存失敗時はログアウトしない |

### 4-4. データ構造
```
localStorage:
  - workHub_accessCode: "abc-123"

Firestore: users/{accessCode}/
  - meta: { createdAt, lastLoginAt }
  - （以下、各機能のサブコレクション）
```

### 4-5. 関連する Never
- CHARTER.md 第9章参照（Firebase Auth、PWA、多端末同時ログイン制御など）

---

## 5. 機能②：日数進捗トラッカー

### 5-1. 機能概要
任意の期間（開始日〜終了日）の進捗を「N日中M日経過」「進捗率%」で可視化。最大10件。1件をピン留めしてヘッダーに常時表示。

### 5-2. 画面（S1：トラッカー一覧）
- 「新規追加」ボタン
- 既存トラッカーのカード一覧（終了日昇順）
- 各カードの内容：
  - タイトル
  - 開始日 〜 終了日
  - 進捗バー
  - 「経過N日 / 全N日（残R日）」
  - ピン留めアイコン（押すとヘッダーに固定）
  - 編集ボタン
  - 削除ボタン
- 追加・編集ダイアログ：
  - タイトル（1〜30文字）
  - 開始日（カレンダー入力）
  - 終了日（カレンダー入力）
  - 保存 / キャンセル

### 5-3. ヘッダー H：ピン留めバー
- PC：1行で「**タイトル｜進捗バー｜残N日/全N日｜47%**」
- スマホ：1行圧縮「**タイトル｜細いバー｜残N日｜47%**」（詳細はタップ展開）
- ピン解除ボタン（×アイコン、1クリック）
- ピン留めゼロ時は **ヘッダーバーを表示しない**

### 5-4. 計算ルール（厳格）

```
進捗率 = (経過日数 / 全日数) × 100

経過日数 = 今日 - 開始日 （0以上、全日数以下にクリップ）
全日数 = 終了日 - 開始日 （正の整数）
残日数 = 終了日 - 今日 （0以上にクリップ、終了日翌日以降は0）

日付計算はすべて日本時間（JST）のカレンダー日で行う。
時刻情報は無視する（00:00:00 として扱う）。
開始日 = 0%（経過0日）、終了日 = 100%（経過 = 全日数）
終了日翌日以降は 100% 固定で「終了」表示。
```

### 5-5. 受け入れ条件

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TRK-01 | タイトル・開始日・終了日を入れて保存 | 一覧に表示される、Firestore に保存される |
| AC-TRK-02 | タイトル空欄で保存 | エラー：「タイトルを入力してください」、保存不可 |
| AC-TRK-03 | タイトル31文字以上 | エラー：「30文字以内で入力してください」 |
| AC-TRK-04 | 開始日 ≥ 終了日 で保存 | エラー：「終了日は開始日より後にしてください」、保存不可 |
| AC-TRK-05 | 開始日 = 終了日 で保存 | エラー（4と同じ） |
| AC-TRK-06 | 11件目を追加しようとする | エラー：「最大10件まで登録できます。不要なものを削除してください」、保存不可 |
| AC-TRK-07 | 進捗率が 0% になる（今日 ≤ 開始日） | 「経過0日 / 全N日」「0%」表示 |
| AC-TRK-08 | 進捗率が 50% になる（半分経過） | 「経過N日 / 全2N日」「50%」表示 |
| AC-TRK-09 | 終了日翌日に開く | 「終了」表示、進捗率100% |
| AC-TRK-10 | 終了日の当日 | 「経過N日 / 全N日」「100%」表示、まだ「終了」ではない |
| AC-TRK-11 | 一覧の並び順 | 終了日昇順（早く終わるものが上） |
| AC-TRK-12 | ピン留めアイコンをタップ | ヘッダー H にそのトラッカーが表示される |
| AC-TRK-13 | 別のトラッカーをピン留め | 前のピン留めが解除され、新しいものに切り替わる |
| AC-TRK-14 | ピン解除（×ボタン） | ヘッダー H が消える（バー自体が非表示） |
| AC-TRK-15 | ピン留め中のトラッカーが終了日翌日になる | ヘッダーに「終了」表示、ピン解除/別を選ぶボタン表示、**自動切替なし** |
| AC-TRK-16 | ピン留め中のトラッカーを削除 | ヘッダー H が消える、ピン留め情報も削除 |
| AC-TRK-17 | 編集ボタンでタイトル・日付を変更 | 一覧と（該当ならヘッダー）の表示が更新される |
| AC-TRK-18 | 削除ボタンを押す | 確認ダイアログ「削除しますか？」→ OK で削除、キャンセルで戻る |
| AC-TRK-19 | リロード後 | Firestore から復元、ピン留め状態も復元 |
| AC-TRK-20 | 別端末で追加 → 自端末で再読み込み | 反映される |
| AC-TRK-21 | スマホ表示でヘッダーバー | 1行圧縮表示、画面上部に固定、**残N日と%が見える** |
| AC-TRK-22 | PC表示でヘッダーバー | 1行通常表示、画面上部に固定、**残N日/全N日と%が見える** |
| AC-TRK-23 | ヘッダーバーのパーセント表示 | 進捗率が整数で表示される（小数点なし、例：47%） |
| AC-TRK-23b | スマホで Todo 編集中（textarea にフォーカス） | **ヘッダー進捗バーが縮小表示**：高さを下げ、**プロジェクト名と進捗バーのみ** 表示。残N日・%・タイトル詳細は省略。本文スペースを最大化 |
| AC-TRK-24 | トラッカー保存が Firestore 失敗 | エラー表示と再試行ボタン |
| AC-TRK-25 | 開始日=今日、終了日=明日（全1日） | 今日 0% / 明日 100% / 「経過0日 / 全1日」 |
| AC-TRK-26 | 端末タイムゾーンが JST 以外 | 日付計算は **明示的に Asia/Tokyo を使用**（dayjs/Intl 等で固定）、結果が変わらない |
| AC-TRK-27 | ピン留めゼロ→1件にピン留め | ヘッダーバーが出現する。**本文エリアのコンテンツがジャンプしない**よう、レイアウト上はスペース確保 or トランジション |
| AC-TRK-28 | ピン留め中トラッカーを別端末で削除 → 自端末で再読込 | ヘッダー H が消え、pinnedTrackerId も null に戻る |
| AC-TRK-29 | ピン留め中トラッカーを削除しようとする | 確認ダイアログに「ピン留め中です。削除すると進捗バーが消えます」を追加表示。OK で削除＋ピン解除 |
| AC-TRK-30 | ピン留め中トラッカー削除時に Firestore 更新失敗 | トラッカー削除と pinnedTrackerId 解除は **Firestore batch write で一括処理**。失敗時は UI を削除前状態に戻し、エラー表示 |

### 5-6. データ構造
```
Firestore: users/{accessCode}/trackers/{trackerId}
{
  id: string,
  title: string,
  startDate: "2026-04-01",  // JST カレンダー日
  endDate: "2026-12-31",
  createdAt: timestamp,
  updatedAt: timestamp
}

users/{accessCode}/meta/
{
  pinnedTrackerId: string | null
}
```

---

## 6. 機能③：Todo（チェック付きアウトラインメモ）

### 6-1. 機能概要
プロジェクト単位でページを分け、各プロジェクト内では **行コンポーネント方式** のチェック付きアウトラインメモを編集する。

### 6-1b. 実装方式（最重要・v0.3 確定）

- **行コンポーネント方式**：`contentEditable` は使わない
- **各行は独立した `textarea` ベース**：
  - 自動リサイズ：内容の長さに応じて高さが拡張される
  - コンテナ幅で見た目だけ折り返す（Word の段落のような挙動）
  - データ上は1行=1テキスト
- **改行コード（`\n`）は `text` フィールドに保存しない**：Enter キーは行分割を意味するため
- 行内のテキスト長制限：**1行あたり最大500文字**（v0.5 改訂、極端なペースト防止）
- プロジェクト全体の容量制限：**900KB**（Firestore 1MB 上限到達前に警告）

### 6-2. 画面（S2：Todo メイン）

**PC レイアウト：**
```
┌─────────┬──────────────────────────┐
│         │ プロジェクト名（編集可）    │
│ プロジ   ├──────────────────────────┤
│ ェクト   │ 保存状態：保存済み 12:34  │
│ サイド   ├──────────────────────────┤
│ バー     │ ☐ 行1                    │
│         │   ☑ 行2（チェック済み）   │
│ [+追加]  │   ☐ 行3                  │
│         │ [+ 新規行]                │
└─────────┴──────────────────────────┘
```

**スマホ レイアウト：**
```
┌──────────────────────────────────┐
│ ▼ プロジェクトA           [≡]    │ ← タブ＋プロジェクト切替メニュー
├──────────────────────────────────┤
│ 保存済み 12:34                    │
├──────────────────────────────────┤
│ ☐ 行1                            │
│   ☑ 行2                          │
│   ☐ 行3                          │
│ [+ 新規行]                       │
└──────────────────────────────────┘
```

### 6-3. データ構造
```
Firestore: users/{accessCode}/todoProjects/{projectId}
{
  id: string,
  name: string,
  lines: [
    {
      id: string,         // 行内一意ID（UI更新用）
      text: string,
      hasCheckbox: boolean,
      checked: boolean,
      indent: number      // 0〜5
    }
  ],
  version: number,        // 競合検知
  updatedAt: timestamp,
  updatedByClientId: string,
  createdAt: timestamp
}
```

### 6-4. 操作（PC）

| 操作 | キー / マウス | 動作 |
|---|---|---|
| 行のテキスト編集 | クリックして入力 | その行が編集モードに |
| 新規行追加（下） | Enter | カーソル位置の下に新規行追加 |
| 行削除 | 行頭 Backspace（行が空のとき） | 行を削除、カーソルは上の行末へ |
| インデント＋ | Tab | その行の indent +1（最大5） |
| インデント− | Shift+Tab | その行の indent −1（最小0） |
| チェックボックス追加/削除 | Ctrl+1 | hasCheckbox トグル |
| チェック ON/OFF | チェックボックスをクリック | checked トグル |

### 6-5. 操作（スマホ）

| 操作 | UI | 動作 |
|---|---|---|
| 行のテキスト編集 | 行をタップ | その行が編集モードに |
| 新規行追加 | ソフトキーボードの改行 or 末尾の「+ 新規行」ボタン | 新規行追加 |
| チェックボックス追加/削除 | 行の右側に表示される小さな「☐」ボタン | hasCheckbox トグル |
| チェック ON/OFF | 既存のチェックボックスをタップ | checked トグル |
| **インデント変更** | **なし（PC限定）** | スマホでは操作不可 |
| **行削除** | **なし（PC限定）** | スマホでは操作不可 |

### 6-6. 保存仕様

| 契機 | 動作 |
|---|---|
| 通常テキスト編集 | **1.5秒デバウンス** で保存 |
| チェックボックスON/OFF | **500ms以内** に保存 |
| プロジェクト切替 | 切替前に **強制保存** |
| `visibilitychange`（タブ非表示） | 保存試行 |
| `pagehide`（ページ離脱） | 保存試行 |
| `beforeunload`（ブラウザ閉じ） | **未保存があれば警告のみ**、保存保証はしない |

### 6-7. 競合検知

```
clientId の生成と保存：
- 初回起動時に UUID をブラウザの localStorage に保存
- 以降は同じブラウザ内（別タブ含む）で同じ clientId を共有
- アクセスコード変更時にはクリア（前ユーザーと混ざらないように）

保存時の手順：
1. 自分が最後に読み込んだ時の version（baseVersion）を保持
2. Firestore transaction で：
   a. サーバー側の最新ドキュメントを取得
   b. サーバー側 version > baseVersion かつ updatedByClientId が自分と異なる場合
      → 警告「他端末で更新されています。再読み込みしてください」
      → 自動上書きしない、編集中の内容は維持
   c. 上記以外は version+1（FieldValue.increment(1) でサーバ側インクリメント）と
      updatedByClientId = 自 clientId で保存
3. 自端末の連続保存は同じ clientId なので競合誤検知しない
```

**保存中ロック：**
- 保存リクエスト送信中は次の保存リクエストをトリガーしない
- 保存中に新たな編集が入ったら、保存完了後にデバウンスを再起動
- visibilitychange / pagehide / beforeunload / プロジェクト切替 / デバウンス到達のいずれかが多重発火しても、ロックで重複保存を防ぐ

### 6-8. 保存状態UI（エディタ上部）

| 状態 | 表示 |
|---|---|
| 保存済み | 「保存済み HH:MM」（薄いグレー） |
| 保存中 | 「保存中...」（点線アニメ） |
| 未保存変更あり | 「未保存の変更あり」（やや警告色） |
| 保存失敗 | 「保存失敗。再試行」+ 再試行ボタン（赤系） |
| 競合警告 | 「他端末で更新されています。再読み込みしてください」+ 再読込ボタン（赤系） |

### 6-9. 受け入れ条件

**プロジェクト管理：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TODO-P01 | 「+追加」ボタンを押す | 新規プロジェクト作成ダイアログが開く |
| AC-TODO-P02 | プロジェクト名（1〜30文字）入力して保存 | サイドバーに追加、選択状態になる |
| AC-TODO-P03 | プロジェクト名空欄で保存 | エラー：「プロジェクト名を入力してください」 |
| AC-TODO-P04 | サイドバーでプロジェクトをクリック | エディタにそのプロジェクトの行が表示される |
| AC-TODO-P05 | プロジェクト名を編集 | サイドバーとヘッダーが更新される |
| AC-TODO-P06 | プロジェクト削除 | 確認ダイアログ → OK で削除、中身も全削除 |
| AC-TODO-P07 | プロジェクト数上限 | **最大100件**。101件目を追加しようとするとエラー |
| AC-TODO-P08 | 現在編集中プロジェクトを未保存状態で削除 | 削除前に「未保存変更も含めて削除しますか？」を表示。OK で保存せず削除、キャンセルで戻る |
| AC-TODO-P09 | プロジェクト削除が Firestore 失敗 | UI 上から消さず、エラー表示。再試行可能 |
| AC-TODO-P10 | 選択中プロジェクト削除後 | 残りプロジェクトがあれば先頭を選択。なければ空状態表示 |
| AC-TODO-P11 | 削除対象が他端末で更新済み | 削除前に最新 version を確認。競合していれば警告し、自動削除しない |
| AC-TODO-P12 | 101件目のプロジェクトを追加 | エラー「最大100件までです」 |

**行編集：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TODO-L01 | 空のプロジェクトを開いた時 | 「+ 新規行」ボタン（**プライマリ・黒背景**）と空状態メッセージ「まだ何も書かれていません。タップして書き始めましょう」を表示 |
| AC-TODO-L02 | 行が1件以上ある状態の「+ 新規行」 | **ターシャリ・薄い文字スタイル**で末尾に表示。押すと新しい行が追加され自動フォーカス |
| AC-TODO-L02b | 各行の textarea 自動リサイズ | コンテンツに応じて高さが拡張、**最大6行分まで**。それ以上は textarea 内部スクロール |
| AC-TODO-L03 | 行のテキストを入力 | 入力できる、1.5秒後に Firestore に保存 |
| AC-TODO-L04 | 連続して入力中 | デバウンスが効いて頻繁に書き込まれない |
| AC-TODO-L05 | 行のどの位置でも Enter | **カーソル位置で行を2つに分割**。分割後の下行にカーソルが移動。元行のカーソル前のテキストは元行に残り、カーソル後のテキストは新しい下行に入る |
| AC-TODO-L06 | 行頭で Backspace（行が空） | その行が削除され、カーソルが上の行末へ |
| AC-TODO-L07 | 行頭で Backspace（行に文字がある） | **上の行と結合**（上行末にカーソルが移動、両行のテキストが連結される。チェック状態とインデントは上の行のものを保持） |
| AC-TODO-L08 | 行で Tab | インデント +1（見た目が右にずれる、最大5まで） |
| AC-TODO-L09 | 行で Shift+Tab | インデント −1（最小0まで） |
| AC-TODO-L10 | 行で Ctrl+1 | hasCheckbox がトグルされる |
| AC-TODO-L11 | チェックボックスをクリック | checked がトグルされ、500ms以内に保存 |
| AC-TODO-L12 | チェック済み行 | 取り消し線＋薄い文字色 |
| AC-TODO-L13 | リロード後 | Firestore から行配列が復元される |
| AC-TODO-L14 | プロジェクト切替 | 切替前に強制保存、新プロジェクトの内容表示 |
| AC-TODO-L15 | 別タブで Todo を開く | 両タブでデータ表示できる（同時編集はサポート外、競合検知で警告） |
| AC-TODO-L16 | IME（日本語入力）変換中の Enter | 変換確定として扱い、新規行追加は発火しない（`compositionstart` / `compositionend` で抑制） |
| AC-TODO-L17 | IME 変換中の Tab | 候補選択として扱い、インデント変更は発火しない |
| AC-TODO-L18 | 行頭以外で Backspace | 通常の1文字削除、行は削除されない |
| AC-TODO-L19 | インデント最大値(5)で Tab | 何も起きない（無視） |
| AC-TODO-L20 | インデント0で Shift+Tab | 何も起きない（無視） |
| AC-TODO-L21 | リッチテキスト（書式付き）をペースト | プレーンテキスト化して挿入 |
| AC-TODO-L22 | 行追加・削除・分割・結合・チェック切替の後 Ctrl+Z（または Cmd+Z） | 1ステップ前の lines 状態に戻る。redo スタックに現状をpush。**1行内のテキスト入力は textarea ネイティブ undo に委譲**。1.5 秒後に通常保存 |
| AC-TODO-L23 | PC で複数行テキストをペースト | **改行で自動分割して複数行追加**（スマホと同じ挙動）。カーソル位置に挿入、最後の行の末尾にカーソル移動 |
| AC-TODO-L24 | 1行あたり**500文字**を超える入力・ペースト | **保存せず、該当行を変更前に戻してエラー表示**。「1行500文字以内で入力してください」。**自動切り詰めはしない** |
| AC-TODO-L25 | hasCheckbox/checked/indent が異なる2行を結合（行頭 Backspace） | 結合後は **上行の hasCheckbox/checked/indent を保持**し、下行の属性は破棄される |
| AC-TODO-L26 | 複数行ペーストで一部の行が500文字超過 | ペースト全体をキャンセルし、エラー表示「N行目が500文字を超えています」。**自動切り詰めはしない** |
| AC-TODO-L28 | プロジェクト全体のサイズが900KBを超える保存 | エラー：「プロジェクトの容量が上限に近づいています（900KB）。古い行を削除するか、新規プロジェクトに分けてください」 |
| AC-TODO-L27 | 1001行目を追加（既存1000行のプロジェクト） | エラー「1プロジェクト最大1000行です。プロジェクトを分けてください」 |
| AC-TODO-L29 | Undo 直後に Ctrl+Y / Cmd+Y（または Ctrl+Shift+Z / Cmd+Shift+Z） | Undo した状態が再適用される（redo） |
| AC-TODO-L30 | Undo履歴が空の状態で Ctrl+Z | preventDefault せず、ブラウザ標準の textarea undo に委譲。**履歴1件のみ残っている時は実行し、結果スタックが空になる。さらに Ctrl+Z を押すと次回はネイティブ undo に委譲** |
| AC-TODO-L31 | 51回目の構造変更 | 最古のUndoエントリが捨てられ、最新50件のみ保持 |
| AC-TODO-L32 | プロジェクト切替後 | Undo / Redo 履歴は完全にクリア（前プロジェクトに戻せない） |
| AC-TODO-L33 | conflict reload（再読み込み）後 | Undo / Redo 履歴クリア（サーバ状態が新基準） |
| AC-TODO-L34 | IME変換中の Ctrl+Z / Ctrl+Y | 何も起きない（compositionstart / end で抑制、変換確定として扱わない） |
| AC-TODO-L35 | 新しい構造変更を行った後 | redo スタックがクリアされる（標準的な undo/redo 動作）。**1行内のテキスト入力は構造変更ではないため redo スタックをクリアしない** |
| AC-TODO-L36 | `saving` / `saving_slow` 状態中の Ctrl+Z / Y | undo / redo は通常通り実行され、結果の lines に対して保存が再デバウンスされる（保存中ロックは onSave のpendingDirtyRef で処理） |
| AC-TODO-L37 | `failed` 状態中の Ctrl+Z / Y | undo / redo は実行可能。undo 後の lines で再度保存を試行（markDirty で 1.5秒デバウンス） |
| AC-TODO-M10 | スマホで Undo / Redo | **専用ボタンを表示しない**。物理キーボード接続時の Ctrl+Z は動作してもよいが積極サポートしない |

**保存状態UI：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TODO-S01 | 編集していない状態 | 「保存済み HH:MM」表示 |
| AC-TODO-S02 | 編集してから1.5秒経たない間 | 「未保存の変更あり」表示 |
| AC-TODO-S03 | 保存中 | 「保存中...」表示 |
| AC-TODO-S04 | ネット切断状態で編集 | 「保存失敗。再試行」表示 + 再試行ボタン |
| AC-TODO-S05 | 再試行ボタンを押す | 再度保存試行、成功なら「保存済み」 |
| AC-TODO-S06 | デバウンス保存とチェックボックス保存が同時にトリガー | 直近の状態を1回の保存にまとめる（重複保存しない） |
| AC-TODO-S07 | 保存中に新たな編集が入った | 保存完了後に再度デバウンスを起動して保存（編集が消えない） |
| AC-TODO-S08 | visibilitychange / pagehide / beforeunload が連続発火 | 保存中フラグでロックし、重複保存しない |
| AC-TODO-S09 | Todoに未保存変更がある状態で別機能タブへ移動／ログアウト | 移動前にユーザーに確認モーダル：「OK=保存して続行 / キャンセル=止まる」。OKの場合 forceSave 試行 → 成功なら移動。**保存失敗時は二次確認**：「OK=変更を破棄して続行 / キャンセル=止まる」（ユーザーがネット切断中等で意図的に作業を中断したい場合の救済）。**conflict 状態は除外**（独立した競合モーダルで解決済みのため、未保存扱いせず移動許可） |
| AC-TODO-S10 | 複数 feature が unsaved guard を登録した時の動作（v3 以降） | guard は登録順（TodoScreen → Leave → Timer）に逐次確認。途中で「止まる」を選んだ時点で全体中止 |

**競合検知：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TODO-C01 | A端末でプロジェクトを開く → B端末で同じプロジェクトを編集して保存 → A端末で保存しようとする | A端末で「他端末で更新されています」警告、上書きされない |
| AC-TODO-C02 | 競合警告の「再読み込み」ボタン | サーバー側の最新を取得して表示、編集中だった内容は失われる |
| AC-TODO-C03 | 自分自身が連続で保存（version インクリメント） | 競合警告を誤って出さない（自分の clientId なら容認） |
| AC-TODO-C04 | 同じプロジェクトを同一ブラウザの別タブで開く | 競合検知は **clientId = ブラウザ単位（localStorage 保存）**、別タブは同一 clientId として扱う。CHARTER の「複数タブ同時編集サポート外」に従う |
| AC-TODO-C05 | transaction が競合・通信断・権限エラー等で失敗 | 保存失敗状態にし、編集中内容は画面に保持。再試行ボタンを表示 |
| AC-TODO-C06 | transaction が自動リトライ中（Firestore の標準リトライ） | ユーザーには「保存中...」を表示し、追加保存リクエストはロックする |
| AC-TODO-C07 | transaction 失敗後に再試行ボタン押下 | 同じ編集中内容で再度 transaction 保存を試みる |
| AC-TODO-C08 | 保存成功後 | クライアント側の baseVersion を保存後 version に更新し、保存済み状態にする |
| AC-TODO-C09 | 同一ブラウザ別タブで同じプロジェクトを同時編集 | 競合検知は保証しない。後から保存した内容が反映される可能性がある。**サポート外として扱う** |

**スマホ動作：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TODO-M01 | スマホで Todo 画面を開く | プロジェクトメニューとエディタが縦に表示される |
| AC-TODO-M02 | スマホで行をタップ | 編集できる、ソフトキーボードが出る |
| AC-TODO-M03 | スマホでチェックボックスをタップ | トグルできる、500ms以内に保存 |
| AC-TODO-M04 | スマホで「+ 新規行」ボタン | 新規行追加、フォーカス移動 |
| AC-TODO-M05 | スマホでインデント変更ボタン | **表示されない** |
| AC-TODO-M06 | スマホで行削除ボタン | **表示されない** |
| AC-TODO-M07 | スマホで複数行をペースト | 改行で1行ずつに分割される（複数行が追加される） |
| AC-TODO-M08 | スマホで PC が作った indent 付きの行をテキスト編集 | indent 情報は保持される（編集対象はテキストのみ） |
| AC-TODO-M09 | スマホで PC が作ったチェックボックス付き行を編集 | hasCheckbox / checked は保持される |

### 6-10. 既知の制限・割り切り
- 複数行をまたぐ範囲選択コピーは不可
- Undo / Redo は構造変更（行追加・削除・分割・結合・チェック・インデント）のみが対象。**1行内のテキスト入力は textarea ネイティブ undo に委譲**（粒度・タイミングは制御しない）
- Undo / Redo 履歴はメモリ上のみ。ページリロード後は復元できない
- Undo / Redo 時にフォーカス位置は復元しない（lines のみ復元、カーソルは別途クリックで配置）
- **素早く乱順に Ctrl+Z / Ctrl+Y を連打した極端ケース**では履歴とUI状態が一時的にズレることがある（通常使用では再現せず、実機テストT1〜T6 でも発生せず）。根本解決にはテキスト入力を履歴に統合する設計変更（C案）が必要だが、現状の B案実装で割り切る
- 編集ロスゼロは保証しない（通信断・電池切れ・OS強制終了時は消える可能性）
- スマホでインデント・行削除はできない
- IME 変換中の Enter / Tab は新規行追加・インデント変更を発火させない（compositionstart/end で抑制）
- リッチテキストペーストはプレーンテキスト化される（書式情報は失われる）
- 複数タブ同時編集はサポートしない（clientId はブラウザ単位＝同一ブラウザの別タブは同 clientId）
- 保存中は次の保存リクエストをロックして重複させない

### 6-11. Undo / Redo（v2b-extra）

#### 対象操作（履歴に積む構造変更）
| 操作 | 履歴記録 |
|---|---|
| 行追加（「+ 新規行」、Enter による分割、複数行ペースト） | ○ |
| 行削除（× ボタン、行頭 Backspace による merge） | ○ |
| checkbox クリック（checked のトグル） | ○ |
| Ctrl+1 による hasCheckbox トグル | ○ |
| Tab / Shift+Tab によるインデント変更（v2c で追加） | ○（自動的に対象） |
| **1行内のテキスト入力（1文字単位）** | **×（textarea ネイティブ undo に委譲）** |
| プロジェクト追加・削除・rename | ×（モーダル経由の確定操作、対象外） |

#### 履歴仕様
- **上限：50ステップ**（プロジェクトごと）。超過時は最古エントリから捨てる
- **保持範囲：メモリ上のみ**（ページリロード・再ログインで消える）
- **クリア条件：**
  - プロジェクト切替時
  - conflict reload（「再読み込み」ボタン）後
  - 新規構造変更が行われた時 → redoスタックのみクリア（標準動作）

#### キーバインド
- **Undo**：`Ctrl+Z` / `Cmd+Z`（Shiftなし）
- **Redo**：`Ctrl+Y` / `Cmd+Y` または `Ctrl+Shift+Z` / `Cmd+Shift+Z`
- **IME入力中は無視**（`compositionstart` / `compositionend` で抑制、L34）
- **履歴が空の場合は preventDefault しない** → ブラウザ標準動作（textareaのネイティブundo）に委譲

#### 保存との連携
- undo / redo による lines 変更も `markDirty()` でデバウンス保存（通常1.5秒）
- conflict 状態では undo / redo を発火しない（編集自体がロックされているため）
- `saving` / `saving_slow` 状態でも undo / redo は実行可能。結果は pendingDirtyRef 経由で保存完了後に再保存される
- `failed` 状態でも undo / redo は実行可能。undo 後の lines で markDirty → 自動再試行
- `visibilitychange` / `pagehide` での強制保存は履歴スタックを読み書きしない（保存だけ）

#### スマホでの扱い
- 専用 Undo / Redo ボタンは表示しない
- スマホからの編集対象は限定的（行追加・テキスト編集・checked切替）なので、誤操作時はサイドバーから別プロジェクトに切替→戻る、または「再読み込み」で復元する運用

---

## 7. 機能④：休暇管理

### 7-1. 機能概要
有給休暇と傷病休暇を**1時間単位**で管理。残量は **計算値のみ**（直接編集不可）。**use（取得）と adjustment（調整）** の2種類のレコードを履歴として保存し、残量を常に再計算。
※ v0.4 までは grant（付与）を別種にしていたが、v0.5 で **adjustment に統合**。年度初めの付与は「note=年度付与」の adjustment レコードとして登録する。

### 7-2. 画面（S3：休暇管理）

```
┌──────────────────────────────────────────────┐
│ 休暇                                          │
├──────────────────────────────────────────────┤
│ 【有給休暇】                                  │
│   残量：15日 4時間                            │
│   今年度取得済み：0日 4時間                  │
│   丸1日取得：0 / 5日（未達、会社ルール目安）   │
│                                              │
│ 【傷病休暇】                                  │
│   残量：4日 0時間                             │
│   今年度取得済み：1日 0時間                   │
│                                              │
│ ※ 今年度 = 2026/9/1〜2027/8/31                │
├──────────────────────────────────────────────┤
│ [ + 取得 ]    ← プライマリ（黒・濃い、メイン）  │
│ [ + 調整 ]    ← セカンダリ（薄い、控えめ）     │
├──────────────────────────────────────────────┤
│ [ 利用履歴 ●][ 調整履歴  ]   ← タブ切替       │
│ 年度フィルタ：[現在年度 ▼]                    │
├──────────────────────────────────────────────┤
│ 2026-12-20  有給  3日 0時間  年末連休  [編集][削除]│
│ 2026-11-15  有給  0日 4時間  午前      [編集][削除]│
│ 2026-10-10  有給  1日 0時間            [編集][削除]│
│ 2026-09-20  傷病  1日 0時間            [編集][削除]│
└──────────────────────────────────────────────┘

調整履歴タブ表示時：
│ 2026-04-01  有給  +20日 0時間  年度付与    [編集][削除]│
│ 2026-04-01  傷病  +5日 0時間   年度付与    [編集][削除]│
│ 2026-03-31  有給  +1日 0時間   繰越        [編集][削除]│
│ 2026-02-15  有給  -0日 2時間   誤計上修正  [編集][削除]│
```

### 7-3. データ構造（v0.7 改訂：use は days+hours、discriminated union）

```
Firestore: users/{accessCode}/leaveRecords/{recordId}

// type === "use" の場合
{
  id: string,
  type: "use",
  leaveType: "paid" | "sick",
  days: integer ≥ 0,               // X日部分（丸1日カウント基準）
  hours: integer 0〜7,              // Y時間部分（半端時間）
  date: "YYYY-MM-DD",              // JST カレンダー日
  note: string | null,             // 任意メモ（30文字まで）
  version: integer,                // 競合検知、初期=1、保存時 +1
  updatedByClientId: string,
  createdAt: timestamp,
  updatedAt: timestamp
}

// type === "adjustment" の場合
{
  id: string,
  type: "adjustment",
  leaveType: "paid" | "sick",
  hours: integer,                  // 正負整数 -9999〜+9999（days フィールドは持たない）
  date: "YYYY-MM-DD",
  note: string | null,             // 任意メモ（用途を書く、例：年度付与、繰越、誤計上修正）
  version: integer,
  updatedByClientId: string,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

**重要：**
- use レコードと adjustment レコードは **discriminated union**（type で判別、フィールド構成が異なる）
- `days` フィールドは **use のみ**（adjustment にはない）
- use の合計時間 = `days * 8 + hours`、adjustment の合計時間 = `hours`

### 7-4. 計算ルール

```
有給残量 = Σ(paid の adjustment.hours) − Σ(paid の use の合計時間)
　ただし use の合計時間 = days * 8 + hours
傷病残量 = sick についても同様

1日 = 8h 固定（換算用）

**休暇画面の表示形式は「X日 Y時間」完全統一**（v0.7 確定）：
  - 0h → 「0日 0時間」
  - 4h → 「0日 4時間」
  - 8h → 「1日 0時間」
  - 9h → 「1日 1時間」
  - 124h → 「15日 4時間」
  - 160h → 「20日 0時間」
  - -2h → 「-0日 2時間」
  - -16h → 「-2日 0時間」

**表示対象：** 残量 / 今年度取得済み / 履歴の use / 履歴の adjustment / 入力モーダル合計プレビュー

入力：
  - use レコード：日数 X（0以上の整数）+ 時間 Y（0〜7の整数）の2フィールド、合計 X*8+Y が1以上
  - adjustment レコード：時間 Z（-9999〜+9999の整数、正負可）の1フィールド

**今年度取得済み（残量サマリ表示用）：**
　有給：Σ(paid use の合計時間) for date ∈ 現在の有給年度（9/1〜翌8/31）
　傷病：Σ(sick use の合計時間) for date ∈ 同じ年度
　※ adjustment は対象外（取得ではないため）

**丸1日有給カウント（5日必須ルール用）：**
　Σ(paid の use.days) for date ∈ 現在の有給年度（9/1〜翌8/31）
　※ use.hours（半端時間）は含めない、days のみカウント
　※ adjustment は対象外
　※ 5日未満なら「今年度取得済み」とは別行に「丸1日取得：◯ / 5日（未達、会社ルール目安）」を warning スタイルで表示
　※ 5日達成なら「丸1日取得：5 / 5日（達成）」と通常スタイルで表示（達成状態も見せる）
```

### 7-5. マイナスチェック（最重要）

```
すべての書き込み操作（新規追加・編集・削除）の保存前に：
1. 当該 leaveType（paid / sick）の残量を再計算
2. 残量 < 0 ならエラー、保存不可
3. 「この操作により残量が ◯h 不足します。保存できません」と表示
```

### 7-6. 履歴タブと年度フィルタ

**2タブ構成：**
- **「利用履歴」タブ**（デフォルト、メイン）：`type === "use"` のレコードのみ表示
- **「調整履歴」タブ**：`type === "adjustment"` のレコードのみ表示

**年度フィルタ（両タブ共通、v0.6 改訂）：**
- ドロップダウン：「現在年度」（9/1〜翌8/31）/「過去年度ごと」/「すべて」
- 年度の境界は **9/1〜翌8/31**（会社固有の有給年度、会計年度4/1〜3/31とは別）
- 例：「2026年度」= 2026/9/1〜2027/8/31
- デフォルト：「現在年度」
- 「すべて」を選択すると年度を跨いだ全レコードを表示

### 7-7. 受け入れ条件

**基本操作：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-01 | 初期状態（レコードなし） | 残量「0日 0時間」表示、両タブの履歴は空 |
| AC-LEAVE-02 | adjustment レコード追加（有給「+ 20日 0時間」, 2026-09-01, note=年度付与） | 有給残量「20日 0時間」表示、調整履歴タブに「+20日 0時間」と追加 |
| AC-LEAVE-03 | adjustment レコード追加（傷病「+ 5日 0時間」, 2026-09-01, note=年度付与） | 傷病残量「5日 0時間」表示 |
| AC-LEAVE-04 | use レコード追加（有給 0日4時間, 2026-05-15） | 有給残量「19日 4時間」表示、利用履歴タブに追加 |
| AC-LEAVE-04b | use レコード追加（有給 3日0時間, 2026-08-01） | 残量から24h減算、利用履歴タブに追加 |
| AC-LEAVE-04c | use レコード追加（有給 3日4時間, 2026-08-01） | 残量から28h減算、利用履歴タブに追加 |
| AC-LEAVE-05 | use レコード追加で残量超過（有給 残19日4時間 のところ 20日0時間 取得を試みる） | エラー：「残量が **0日 4時間** 不足します。保存できません」、保存不可 |
| AC-LEAVE-06 | use レコード編集で残量超過 | エラー、編集不可 |
| AC-LEAVE-07 | adjustment レコード削除で残量がマイナスに | エラー、削除不可 |
| AC-LEAVE-08 | adjustment レコード追加（有給「+ 1日 0時間」繰越） | 有給残量に「1日 0時間」加算、調整履歴タブに「+1日 0時間」と追加 |
| AC-LEAVE-09 | adjustment レコード追加（有給「- 0日 5時間」誤計上修正） | 有給残量から「0日 5時間」減算。操作後の合計残量が 0 以上なら保存OK、0未満ならエラーで保存不可 |
| AC-LEAVE-10 | use または adjustment の入力欄に小数（4.5） | エラー：「整数で入力してください」、保存不可 |
| AC-LEAVE-11 | adjustment の合計が 0（日数=0 かつ 時間=0） | エラー：「日数または時間を入力してください」、保存不可 |
| AC-LEAVE-12 | use または adjustment の入力欄が空 | エラー：「数値を入力してください」、保存不可 |
| AC-LEAVE-13 | 日付未入力 | エラー：「日付を選択してください」、保存不可 |
| AC-LEAVE-14 | note 31文字以上 | エラー：「メモは30文字以内で入力してください」 |
| AC-LEAVE-15 | use の days または hours が負の値、または hours が 8 以上 | エラー：「日数は0以上、時間は0〜7の整数で入力してください」、保存不可（合計時間は正のみ） |
| AC-LEAVE-16 | 履歴の並び順（両タブとも） | date 降順、同日の場合は createdAt 降順 |
| AC-LEAVE-17 | 履歴の編集ボタン | ダイアログが開き、type 以外（leaveType, hours, date, note）を変更可能 |
| AC-LEAVE-18 | 履歴の削除ボタン | 確認ダイアログ → OK で削除、残量再計算 |
| AC-LEAVE-19 | リロード後 | Firestore から全レコード復元、残量も正しく再計算 |
| AC-LEAVE-20 | 別端末で追加 → 自端末で再読込 | 反映される |
| AC-LEAVE-21 | 残量表示の換算（160h） | 「20日0時間」 |
| AC-LEAVE-22 | 残量表示の換算（9h） | 「1日1時間」 |
| AC-LEAVE-23 | 残量表示の換算（0h） | 「0日0時間」 |
| AC-LEAVE-24 | use の入力形式 | **「X日 Y時間」の2フィールド入力**。X日：0以上の整数、Y時間：0〜7の整数。合計（X*8+Y）が1以上、かつ残量を超えなければ保存OK |
| AC-LEAVE-25 | adjustment の hours 範囲 | **-9999〜+9999 の整数**。範囲外はエラー |
| AC-LEAVE-26 | 同一日・同一leaveType・同一type のレコード追加 | 重複許可（午前/午後で別記録可能）。警告も出さない |
| AC-LEAVE-27 | adjustment 編集・削除により過去の use 合計が残量超過になる | 保存不可。**メッセージ「先に利用履歴を修正してください。この調整を変更すると残量が ◯h 不足します」を表示** |
| AC-LEAVE-28 | 年度フィルタを変更 | **履歴一覧のみ絞り込む**。残量サマリは常に全レコード累計の現在残量を表示。UI に小さく「残量は全期間累計」と注記 |
| AC-LEAVE-29 | use 1レコードで複数日入力（例：3日 4時間） | 1レコードとして保存される（28h 相当）。長期休暇を1レコードで入力可能（v0.7 改訂） |
| AC-LEAVE-30 | 同日同種の use を追加 | 保存は許可するが、note 入力欄に「午前」「午後」等の入力を推奨する補助テキストを表示（プレースホルダ等） |
| AC-LEAVE-31 | adjustment で絶対値が **10日 0時間（80時間相当）以上** の値を保存 | 確認ダイアログ「大きな調整です。**◯日 ◯時間 を保存しますか？**」を表示。OK で保存、キャンセルで戻る |
| AC-LEAVE-32 | 現在年度に adjustment レコードが1件もない | 残量サマリ付近に控えめに「現在年度の調整レコードがありません。年度付与を入力してください」と表示 |
| AC-LEAVE-33 | use 入力時 X日が負の整数 | エラー：「日数は0以上で入力してください」、保存不可 |
| AC-LEAVE-34 | use 入力時 Y時間が0〜7の範囲外（例：8 や -1） | エラー：「時間は0〜7の整数で入力してください。8時間以上は日数欄を使ってください」、保存不可 |
| AC-LEAVE-35 | use 入力時 X日=0 かつ Y時間=0 | エラー：「日数または時間を入力してください」、保存不可 |
| AC-LEAVE-36 | use 入力時 X日・Y時間ともに整数以外（小数・空欄） | エラー：「整数で入力してください」、保存不可 |

**有給5日必須ルール（v0.7 再設計）：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-Y01 | 有給年度の境界判定 | 9/1〜翌8/31 を1年度として扱う（例：2026/9/1〜2027/8/31 = 「2026年度」） |
| AC-LEAVE-Y02 | 履歴の年度フィルタ表示 | 「現在年度（YYYY年度=9/1〜翌8/31）」「過去年度」「すべて」から選択 |
| AC-LEAVE-Y03 | 今年度取得済み（有給）の集計 | 現在の有給年度内の `paid` かつ `use` レコードの **合計時間**（days*8 + hours）の総和を表示 |
| AC-LEAVE-Y04 | 今年度取得済み（傷病）の集計 | 現在の有給年度内の `sick` かつ `use` レコードの合計時間の総和を表示 |
| AC-LEAVE-Y05 | 丸1日有給カウントの集計 | 現在の有給年度内の `paid` use レコードの `days` フィールド合計（hours 部分は無視） |
| AC-LEAVE-Y06 | 丸1日取得が5日未満（有給のみ） | 「今年度取得済み」とは**別行に**「丸1日取得：◯ / 5日（未達）」を warning スタイルで表示 |
| AC-LEAVE-Y07 | 丸1日取得が5日以上達成 | 「丸1日取得：5 / 5日（達成）」と表示（達成状態も見せる、消滅させない）。warning スタイル解除、通常色に |
| AC-LEAVE-Y08 | 5日未満で年度を跨いだ場合 | 新年度になったら丸1日カウントリセット、「丸1日取得：0 / 5日（未達）」再表示 |
| AC-LEAVE-Y09 | 5日ルールは有給のみ対象 | 傷病休暇には「丸1日取得」行を表示しない（残量 + 今年度取得済みのみ） |
| AC-LEAVE-Y10 | adjustment は丸1日カウントに含めない | 「+1日 0時間 年度付与」等の adjustment は丸1日カウントに加算しない（use のみ） |
| AC-LEAVE-Y11 | 初回利用時の表示 | 残量「0日 0時間」、今年度取得済み「0日 0時間」、丸1日取得「0 / 5日（未達）」。**過去分入力前提**：会社有給年度内に既に取得済みの丸1日有給がある場合は、過去分 use として入力する必要あり。過去分未入力なら警告は不正確 |
| AC-LEAVE-Y12 | 過去分 use の入力 | 過去日付の use レコードを追加すると、その日付が現在の会社有給年度内なら「今年度取得済み」「丸1日取得」に反映される |
| AC-LEAVE-Y17 | 過去年度の use を入力した場合 | 現在年度サマリ（残量除く）には反映しない。該当年度の履歴フィルタで確認する旨を画面に注記 |
| AC-LEAVE-Y13 | 1レコードで複数日入力した場合の5日達成判定 | 1レコード「5日0時間」の use 入力 → days 合計 = 5 → 5日達成、警告即時消滅 |
| AC-LEAVE-Y14 | 半端時間のみのレコード | 1レコード「4日7時間」（合計 39h）→ days = 4、5日カウント = 4、警告継続表示 |
| AC-LEAVE-Y15 | 複数レコードで5日達成 | 「4日0時間」+「1日0時間」の2レコード → days 合計 5、5日達成 |
| AC-LEAVE-Y16 | 5日達成判定式 | `Σ(use.days where leaveType=paid, date∈現在の有給年度) >= 5` で達成。`< 5` で警告表示 |

**use レコード編集時の AC（v0.7 追加）：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-E01 | use 編集ダイアログを開く | 編集前の days, hours, leaveType, date, note が初期値として復元される |
| AC-LEAVE-E02 | use 編集で days と hours の両方を変更 | 合計時間が再計算され、残量に反映。マイナスならエラー |
| AC-LEAVE-E03 | use 編集で leaveType を変更（有給→傷病） | 旧 leaveType の残量に戻り、新 leaveType から減算。両方マイナスにならないこと |
| AC-LEAVE-E04 | use 編集で days を 5日以上にする（丸1日カウント増） | 「今年度取得済み」「丸1日カウント」即時再計算、5日達成なら警告消滅 |
| AC-LEAVE-E05 | use 編集で days を減らす（丸1日カウント減） | 丸1日カウント < 5 なら警告再表示 |
| AC-LEAVE-E06 | use 編集で date を変更（年度跨ぎ） | 元の年度の集計から消え、新年度の集計に加算 |
| AC-LEAVE-E07 | use 編集で合計時間 0（days=0, hours=0）にする | エラー：「日数または時間を入力してください」、保存不可 |
| AC-LEAVE-E08 | use 編集（変更なしで保存ボタン） | updatedAt のみ更新、version も +1（実装簡略化のため） |

**adjustment 編集時の AC（v0.7 追加）：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-A01 | adjustment 編集ダイアログを開く | 内部 signed hours から「符号 + X日 + Y時間」に正しく復元される（変換ルールに従う） |
| AC-LEAVE-A02 | adjustment 編集で符号を反転（+ → -） | 内部 hours の符号が反転、残量への影響が逆転 |
| AC-LEAVE-A03 | adjustment 編集で大きな調整に変更 | 絶対値 80h 以上なら確認ダイアログ |
| AC-LEAVE-A04 | adjustment 編集で残量がマイナスになる | エラー：「先に利用履歴を修正してください。この調整を変更すると残量が ◯日 ◯時間 不足します」 |
| AC-LEAVE-A05 | adjustment UI 復元の境界（内部 hours = -7） | UI 「-」「0日」「7時間」（abs=7, 7÷8=0余7） |
| AC-LEAVE-A06 | adjustment UI 復元の境界（内部 hours = -10） | UI 「-」「1日」「2時間」（abs=10, 10÷8=1余2） |

**年度跨ぎ・初回利用 AC（v0.7 追加）：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-B01 | 9/1 0:00 を跨いだ瞬間（画面開きっぱなし） | 自動更新は保証しない。**タブ切替・再読み込み時に新年度の集計に切り替わる**。useMemo の依存配列に getCurrentLeaveYear() を含めない（古い値が固定される問題を許容） |
| AC-LEAVE-B02 | 初回利用時の警告表示順位 | 「現在年度の adjustment レコードなし」警告（AC-LEAVE-32）と「丸1日取得 5日未達」（AC-LEAVE-Y11）が同時発火 → **AC-LEAVE-32 が優先表示**（adjustment を入れないと残量0で何もできないため）、Y11 はその後 |
| AC-LEAVE-B03 | 初回の表示構成 | 有給カードに「今年度取得済み：0日 0時間」+「丸1日取得：0 / 5日（未達）」の2行を別々に表示。傷病カードには「丸1日取得」行を表示しない |
| AC-LEAVE-B04 | 「X日 Y時間」のスペース表記 | **数字と「日」「時間」の間に半角スペースを入れる**（例：「15日 4時間」「-2日 0時間」「+20日 0時間」）。負号「-」と数字の間にはスペース無し |

**過去年度フィルタの動的選択肢 AC（v0.7 追加）：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-F01 | 「過去年度ごと」の選択肢生成 | leaveRecords 内（**use と adjustment 両方**）の date を走査し、現在年度より古い年度を **動的に列挙**（例：「2025年度」「2024年度」） |
| AC-LEAVE-F02 | 過去年度のレコードが1件も無い場合 | 選択肢に過去年度を出さない（「現在年度」と「すべて」のみ） |
| AC-LEAVE-F03 | 過去年度を選択中、その年度のレコード全削除 | 選択肢から消える、自動で「現在年度」に戻る |
| AC-LEAVE-F04 | 「すべて」選択時 | 全期間のレコードを年度関係なく表示 |

**タブ・フィルタ・UI階層：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-LEAVE-T01 | 画面遷移時のデフォルトタブ | 「利用履歴」タブが選択されている |
| AC-LEAVE-T02 | 「利用履歴」タブを選択 | use レコードのみ表示 |
| AC-LEAVE-T03 | 「調整履歴」タブを選択 | adjustment レコードのみ表示 |
| AC-LEAVE-T04 | 年度フィルタ「現在年度」 | 当該有給年度の **9/1〜翌8/31** のレコードのみ表示 |
| AC-LEAVE-T05 | 年度フィルタ「すべて」 | 全レコード表示 |
| AC-LEAVE-T06 | 年度フィルタ「2025年度」 | **2025/9/1〜2026/8/31** のレコードのみ |
| AC-LEAVE-T07 | 「+ 取得」ボタンのスタイル | プライマリ（黒背景・白文字、目立つ） |
| AC-LEAVE-T08 | 「+ 調整」ボタンのスタイル | セカンダリ（薄いグレー背景・黒文字、控えめ） |

### 7-8. 操作モーダルの仕様

**新規追加ダイアログ（取得 / 調整、v0.6 改訂）：**

【取得ダイアログ（type=use 固定）】
- 休暇種別（有給/傷病）：ラジオボタン
- **日数（X）**：数値入力、0以上の整数、デフォルト 1
- **時間（Y）**：数値入力、0〜7の整数、デフォルト 0
- 入力欄の補足：「合計 = X日 × 8時間 + Y時間 = Zh」と自動計算表示
- 日付：日付ピッカー、デフォルト=今日
- メモ：任意テキスト（30文字以内、同日同種ある時は「午前/午後」推奨プレースホルダ）
- 保存ボタン：クリック時に残量再計算 → マイナスチェック
- キャンセルボタン

【調整ダイアログ（type=adjustment 固定、v0.7 改訂：3フィールド入力）】
- 休暇種別（有給/傷病）：ラジオボタン
- **正負トグル**：「+」または「-」を選択（プラスは加算、マイナスは減算）
- **日数（X）**：数値入力、0以上の整数、デフォルト 0
- **時間（Y）**：数値入力、0〜7の整数、デフォルト 0
- 入力欄の補足：「合計 = 符号 × (X*8 + Y) = ±Z時間」と自動計算表示
- 日付：日付ピッカー、デフォルト=今日
- メモ：任意テキスト（30文字以内、用途を書くことを推奨、例：「年度付与」「繰越」「誤計上修正」）
- 保存ボタン：クリック時に残量再計算 → マイナスチェック
- キャンセルボタン

**内部変換ルール（重要）：**
- 入力 UI 「正負トグル + X日 + Y時間」 → 内部 `hours: int (signed)`
- 変換式：`hours = sign × (days × 8 + hours_input)`、where `sign ∈ {+1, -1}`
- 例：「+」「20日」「0時間」 → 内部 `hours: +160`
- 例：「-」「0日」「5時間」 → 内部 `hours: -5`
- 例：「-」「1日」「2時間」 → 内部 `hours: -10`
- 編集時に内部 hours から UI への復元：
  - `sign = hours >= 0 ? '+' : '-'`
  - `abs = Math.abs(hours)`
  - `days = Math.floor(abs / 8)`
  - `hours_input = abs % 8`
- 「+」かつ X=0, Y=0 や「-」かつ X=0, Y=0 は保存不可（AC-LEAVE-11）

**use ダイアログとの違い：**
- use：日数 X（≥0）+ 時間 Y（0〜7）、符号トグルなし（常に正の取得）
- adjustment：符号トグル + 日数 X（≥0）+ 時間 Y（0〜7）、内部 signed hours

**adjustment モーダルのレイアウト・プレビュー（v0.7 追加）：**
- 符号トグル「+」「-」・日数入力・時間入力を **1行にまとめて表示**（離れないように）
- 「+」のラベル：「**追加**」、「-」のラベル：「**減算**」
- 保存ボタンの直前に **プレビュー文** を表示：
  - 「+」かつ X=20, Y=0 → 「残量に **+20日 0時間** 反映」（緑系）
  - 「-」かつ X=0, Y=5 → 「残量から **0日 5時間** 減算」（赤系）
  - これによりユーザーがマイナス調整を誤入力しても直前で気づける

**adjustment の符号 0 ハンドリング（v0.7 追加）：**
- X=0 かつ Y=0 のとき：符号トグルは表示上のみ有効、保存不可（AC-LEAVE-11）
- 内部で hours=0 として保存されることはない（Security Rules でも hours != 0 を強制）
- 編集時に万一 hours=0 の不正データを読み込んだ場合：符号は「+」として表示、保存時にエラー

**残量超過時の運用ガイダンス（v0.7 追加）：**
- use で残量超過エラーが出た場合、メッセージに次の選択肢を提示：
  - 「先に **+ 調整** で残量を補正してから取得を入力する」
  - 「既存の利用履歴を修正する」
- 「現実の会社残量とアプリの残量がズレている場合」の解決導線として明示

**編集ダイアログ：**
- type は変更不可（保存時の整合性のため）
- それ以外は新規と同じ

---

## 8. 機能⑤：タイマー

### 8-1. 機能概要
作業時間を可視化する**端末ローカル完結**の表示装置。カウントアップ／カウントダウンの2モード。履歴なし。Firestore 書き込みなし。

**コアバリュー：「今このセッションの経過を可視化する」**。記録・分析・集計はしない。

### 8-2. 画面（S4：タイマー）

```
┌──────────────────────────────────┐
│ モード：[●カウントアップ] [○カウントダウン] │
├──────────────────────────────────┤
│                                  │
│        00:25:34                  │ ← 大きく表示
│                                  │
├──────────────────────────────────┤
│ [Start] [Stop] [Reset]            │
└──────────────────────────────────┘
```

**カウントダウンモード時の追加：**
- 設定時間入力欄（**MM:SS 形式、1:00〜99:59**、デフォルトは前回値）

### 8-3. 計測方式

```js
// 開始時に開始時刻を localStorage に保存
localStorage.setItem('timerStartedAt', Date.now())

// 表示更新（1秒ごとに）
const elapsed = Math.floor((Date.now() - startedAt) / 1000)
display = formatSeconds(elapsed)
```

**setInterval のカウントは使わない**。バックグラウンドからの復帰でズレないようにするため、常に Date.now() の差分で計算する。

### 8-4. データ管理

```
localStorage:
  - timerMode: "up" | "down"
  - timerState: "running" | "stopped" | "finished"
  - timerStartedAt: timestamp（実行中のみ）
  - timerCountdownSeconds: number（カウントダウンの設定値）
  - timerLastCountdownSeconds: number（次回のデフォルト用）
```

### 8-5. タブ閉じ・再訪時の挙動

```
タブを閉じた時：
  - 保存・停止処理は保証しない
  - localStorage には timerState="running" が残ったまま

次回アプリを開いた時：
  - timerState を読み込む
  - "running" だった場合：
    - 「前回のタイマーが起動したままでした。停止状態として復帰します」と表示
    - timerState を "stopped" に変更、startedAt をクリア
    - 経過時間は失われる（保存しない）
```

### 8-6. カウントダウン到達時の挙動（v0.5：音追加）

```
elapsed >= countdownSeconds になったら：
1. 表示を 00:00 に固定
2. 表示色を赤系に変更
3. 画面内に「時間です」テキスト表示
4. document.title を「⏰ 時間です | work-hub」に変更
5. timerState を "finished" に
6. 設定で音 ON の場合、短い通知音を1回再生（Web Audio API）
7. 音の ON/OFF 設定はタイマー画面に「🔔 音 ON / OFF」トグルを置く
8. OS通知・ブラウザ通知は出さない
```

**音の仕様：**
- 短い1音（500ms 程度）、Web Audio API で生成 or 軽量 mp3
- 音量はデバイス標準
- 音の種類は v4 実装時に試聴して決定（候補：ピン / ベル / チン）
- 設定は localStorage に保存（`timerSoundEnabled: true/false`、デフォルト true）

### 8-7. 受け入れ条件

**カウントアップ：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TMR-U01 | カウントアップモードを選択 | モードラジオが切り替わる、表示が 00:00:00 |
| AC-TMR-U02 | Start を押す | 00:00:00 から1秒ごとにカウントアップ |
| AC-TMR-U03 | Start 後、別タブに切替 → 1分後に戻る | 約 1:00 を正しく表示（バックグラウンドでもズレない） |
| AC-TMR-U04 | Stop を押す | 表示が停止、状態は "stopped" |
| AC-TMR-U05 | Stop 後に Start を押す | 停止時点から再開 |
| AC-TMR-U06 | Reset を押す | 00:00:00 にリセット、状態は "stopped" |
| AC-TMR-U07 | Reset 確認ダイアログ | 「リセットしますか？」→ OK で実行、キャンセルで戻る |
| AC-TMR-U08 | カウントアップ中にカウントダウンモードに切替 | 確認「現在の計測を破棄しますか？」→ OK で切替、キャンセル時は実行中タイマー維持 |
| AC-TMR-U09 | 実行中に Reset ボタンを押す | **確認ダイアログ「リセットしますか？」→ OK で即停止＆リセット**、キャンセルで実行継続 |
| AC-TMR-U10 | Start ボタンの濃淡（停止中） | **プライマリ**（黒背景・白文字、目立つ） |
| AC-TMR-U11 | Stop ボタンの濃淡（実行中） | **プライマリ**（黒背景・白文字、目立つ。実行中は Start ではなく Stop が主役になる） |
| AC-TMR-U12 | 待機状態の Stop ボタン | **ターシャリ**（透明、停止中はそもそも押せないがあれば薄い表示） |
| AC-TMR-U13 | 実行中の Start ボタン | **ターシャリ**または非表示（実行中は Start ではなく Stop を押すべき） |

**カウントダウン：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TMR-D01 | カウントダウンモードを選択 | 設定時間入力欄が表示される（デフォルト 25:00） |
| AC-TMR-D02 | 設定時間を 5:00 に変更して Start | 5:00 から1秒ごとにカウントダウン |
| AC-TMR-D03 | Stop を押す | 表示が停止、状態は "stopped" |
| AC-TMR-D04 | Stop 後に Start | 停止時点から再開 |
| AC-TMR-D05 | カウントダウンが 00:00 に到達（音ON設定時） | 自動停止、表示赤、「時間です」表示、タブタイトル変更、**短い通知音を1回再生** |
| AC-TMR-D05b | カウントダウンが 00:00 に到達（音OFF設定時） | 自動停止、表示赤、テキストとタブタイトル変更のみ、**音は鳴らない** |
| AC-TMR-D05c | タイマー画面に「🔔 音 ON / OFF」トグル表示 | クリックで切替可能、状態は localStorage に保存 |
| AC-TMR-D05d | 音設定のデフォルト値 | デフォルトは ON |
| AC-TMR-D12 | カウントダウン中に端末がスリープ → 復帰 | バックグラウンド中も Date.now() 差分で計算。到達済みなら起動直後に `finished` 状態で復帰、ただし**音は鳴らさない**（既に過ぎているため） |
| AC-TMR-D13 | システム時計が手動変更されて Date.now() が巻き戻った | 経過時間を **max(0, elapsed) でクランプ**。負の値を表示しない |
| AC-TMR-D14 | 音設定が localStorage に保存できない（プライベートモード等） | デフォルト ON のメモリ保持で動作、永続化エラーは無視 |
| AC-TMR-D15 | カウントダウン実行中に「設定時間」入力欄を編集 | **即時に残り時間を再計算**。入力した新しい時間 − 既に経過した時間 = 新しい残り時間（負ならその場で finished） |
| AC-TMR-D16 | カウントダウン実行中に設定時間を 既経過時間より短く設定 | 残り時間が負になるので、その場で 0:00 に到達し finished 状態。**ただし設定編集起因の場合は通知音を鳴らさない**（視覚通知とタブタイトル変更のみ） |
| AC-TMR-D17 | finished 後に Reset | document.title を通常の「work-hub」に戻す。表示色・テキストも通常に戻す |
| AC-TMR-D18 | finished 状態でログアウト | document.title を通常の「work-hub」に戻す |
| AC-TMR-D19 | finished 状態で音 OFF に切替 | 表示・タイトルは維持、音だけ以後鳴らない（既に鳴り終わっている） |
| AC-TMR-D20 | 音 ON の状態で Start を押す | **ユーザー操作内（onClick ハンドラ内）で AudioContext を作成または resume**し、通知音再生の準備を行う。iOS Safari の自動再生制限対策 |
| AC-TMR-D21 | AudioContext 初期化に失敗 | タイマー自体は開始する。「音は鳴らない可能性があります」と控えめに1回表示 |
| AC-TMR-D22 | 音 ON でもブラウザ制約で再生失敗 | 視覚通知とタブタイトル変更は必ず行い、音失敗はアプリ停止扱いにしない |
| AC-TMR-D23 | 画面に小さく音 ON 状態を明示 | 「音 ON：終了時に短い音が鳴ります」をタイマー画面のどこかに表示（オフィスで突然鳴って驚かないため） |
| AC-TMR-D06 | 到達後に Reset | 設定時間に戻る、色も通常に |
| AC-TMR-D07 | 設定時間 0:00 で Start | エラー：「1分以上を設定してください」 |
| AC-TMR-D08 | 設定時間 99:59 を超える | **MM:SS 形式、上限 99:59（5999秒、約100分）**。それ以上は入力不可 |
| AC-TMR-D09 | 設定時間入力が小数 | 小数入力を受け付けない（整数のみ） |
| AC-TMR-D10 | 設定時間が前回値で復元 | 前回設定した値が localStorage から読み込まれる |

**タブ閉じ・再訪：**

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-TMR-X01 | Start 中にタブを閉じる → 再度開く | 「前回のタイマーが起動したままでした。停止状態として復帰します」表示、状態は "stopped" |
| AC-TMR-X02 | Stop 中にタブを閉じる → 再度開く | 状態は "stopped" のまま、メッセージ表示なし |
| AC-TMR-X03 | カウントダウン到達後にタブを閉じる → 再度開く | 状態 "finished" のまま復元、もう一度 Reset で初期化 |
| AC-TMR-X04 | 別端末で別のタイマーを動かしている | 端末ごとに独立、相互に影響しない |
| AC-TMR-X05 | Firestore に書き込みが発生していない | ネットワークタブで確認、タイマー操作中 Firestore リクエストゼロ |

---

## 9. 共通仕様

### 9-0. 共通部品の受け入れ条件（v1 で土台を作る）

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-COMMON-01 | Firestore 読み込み失敗 | 共通エラー表示「データの取得に失敗しました。再読み込みしてください」を表示 |
| AC-COMMON-02 | Firestore 書き込み失敗 | 対象機能ごとのエラー表示と再試行導線を出す |
| AC-COMMON-03 | localStorage 利用不可（プライベートモード等） | アプリが白画面にならず、必要箇所はメモリ上のデフォルトで動作（保存先がなくなる旨を控えめに表示） |
| AC-COMMON-04 | Button variant | primary / secondary / tertiary が全画面で同じ見た目になる（共通 Button コンポーネント経由） |
| AC-COMMON-05 | JST 日付計算 | 端末タイムゾーンに依存せず、**共通ユーティリティ経由で Asia/Tokyo として処理**される |

### 9-0b. データマイグレーション AC

| # | 受け入れ条件 | 期待結果 |
|---|---|---|
| AC-DATA-01 | 既存ユーザーが v3 追加後に休暇画面を初めて開く | leaveRecords が空でもエラーにならず、残量「0日 0時間」として表示 |
| AC-DATA-02 | meta に未知フィールドがある（古いアプリ + 新しいフィールド等） | 無視してアプリが動作する |
| AC-DATA-03 | 旧バージョンで存在しないサブコレクションを読む | 空配列として扱い、**Firestore に空データを上書き作成しない**（既存データを破壊しない） |


### 9-1. ナビゲーション

**PC（1280px〜）：**
- 左サイドナビ：トラッカー / Todo / 休暇 / タイマー
- 上部に固定ヘッダー（H：ピン留めバー）

**スマホ（375px〜414px）：**
- 下部にボトムナビ：4タブ
- 上部に固定ヘッダー（H：1行圧縮ピン留めバー）

### 9-1b. ボタン濃淡の3階層（全画面で統一）

| 階層 | 用途 | スタイル | 適用例 |
|---|---|---|---|
| **プライマリ** | よく使う・主要アクション | 黒背景 + 白文字 | トラッカー新規追加 / **空プロジェクトの「+新規行」** / 休暇「+取得」 / タイマー Start（停止中） |
| **セカンダリ** | 時々使う・補助アクション | 薄グレー背景 + 黒文字 | 休暇「+調整」 / タイマー Stop / 編集ダイアログの保存 |
| **ターシャリ** | あまり使わない・キャンセル系 | 透明背景 + グレー文字 | キャンセル / ピン解除 / 「もっと見る」 / Reset / **行が1件以上ある状態の Todo「+新規行」** |

機能ごとにスタイルがバラバラにならないように、共通の `Button` コンポーネントで variant を持つ。

**例外：タイマーの Start/Stop は文脈で入れ替える**（停止中は Start=プライマリ、実行中は Stop=プライマリ）。「今押したいボタン」が常に一番目立つようにする UX 配慮。

### 9-2. 機密データ前提（注意書き表示はしない）
- 「社外秘・機密情報を入力しない」のはユーザー本人の自己管理ルール
- 画面上に注意書きは出さない（ユーザー要望により撤廃）

### 9-3. ローディング・空状態・エラー状態

| 状態 | 表示 |
|---|---|
| 初回ロード中 | 「読み込み中...」スピナー |
| 各機能が空 | 各機能ごとの空状態メッセージ（例：「まだトラッカーがありません。新規追加してみましょう」） |
| Firestore 通信失敗 | 「データの取得に失敗しました。再読み込みしてください」+ 再読込ボタン |
| ログアウト後 | S0 に戻る |

### 9-4. 404 ページ
- 想定外URLアクセス → S1 にリダイレクト
- もしくは「404」表示 + S1 へのリンク

### 9-5. データ容量の見積もり（無料枠保護）
- 1ユーザーのデータ規模見積もり：
  - トラッカー：最大10件 × 数百バイト = 数KB
  - Todo：プロジェクト数 × 行数。1プロジェクト最大1MB（Firestore上限）。
  - 休暇レコード：1年で 数百件程度
  - 合計：数MB以内
- Firestore Spark プランの 1GB は十分余裕

### 9-6. テーマカラー（v0.5：モノクロ・ミニマル方針確定）

**方針：ChatGPT / Apple 風のモノクロ・ミニマル。v1 から固定、v5 で磨き込み。**

```css
/* 基本（モノクロ） */
--color-bg: #ffffff;                /* 白地 */
--color-surface: #f7f7f8;           /* 薄いグレー（カード背景） */
--color-text: #0d0d0d;              /* ほぼ黒 */
--color-text-muted: #6b7280;        /* ミュート文字 */
--color-border: #ececec;            /* 罫線 */

/* ボタン3階層 */
--color-primary-bg: #18181b;        /* プライマリ：濃い黒 */
--color-primary-fg: #ffffff;        /* プライマリ：白文字 */
--color-secondary-bg: #f3f4f6;      /* セカンダリ：薄いグレー */
--color-secondary-fg: #18181b;      /* セカンダリ：黒文字 */
--color-tertiary-bg: transparent;   /* ターシャリ：透明 */
--color-tertiary-fg: #6b7280;       /* ターシャリ：グレー文字 */

/* ステータス（限定使用） */
--color-success: #10b981;
--color-warning: #f59e0b;
--color-error: #ef4444;

/* アクセント（必要最小限） */
--color-accent: #3b82f6;            /* Apple Blue 系、リンクやフォーカス時のみ */

/* 余白・角丸・フォント */
--radius-sm: 6px;
--radius-md: 8px;
--radius-lg: 12px;
--font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans JP", sans-serif;
```

**密度・タイポグラフィ規定（モノクロ UI で密度が崩れないように）：**

```css
/* テキストサイズ */
本文フォントサイズ：14px or 15px
主要数値表示（残N日 / 残量h など）：32px 以上
見出し（プロジェクト名 / 画面タイトル）：18〜20px

/* 余白・行間 */
カード内余白：12〜16px
行間：1.5前後
セクション間：24〜32px

/* 罫線・影 */
罫線：1px solid var(--color-border) （編集行の境界は罫線で明示）
影：原則使わない。使う場合もヘッダー / モーダルのみ、控えめに

/* 色の使い分け（モノクロ方針を壊さないルール） */
通常状態・保存済み：色を使わない（グレースケールのみ）
色を使うのは：エラー・警告・タイマー終了通知のみ
「保存済み」は緑にせず、グレーのテキストで表現
チェック済み行は色変更ではなく取り消し線＋薄文字で表現

/* 絵文字とアイコンのルール（v0.4 追加） */
絵文字（🔥📅☐🌴⏱⚙📌✏️🗑🔔⏰等）は一切使わない（ダサく見える）
アイコンが必要な箇所は SVG（Lucide / Heroicons の単純な線アイコン）またはテキストラベル
チェックボックスは CSS で四角＋チェックマークを描画（Unicode の☐や✓は使わない）

/* ボタンのテキスト改行禁止 */
すべてのボタンに white-space: nowrap を設定
ボタンに min-width を確保し「キャンセル」「リセット」などが改行されないようにする
スマホでは特に幅不足になりやすいため、min-width と padding を再検証
```

v5 ではこの方針を維持しつつ細部を磨き込む。色調を大きく変えない。

---

## 10. やらないこと参照

「やらないこと」リストは CHARTER.md 第9章を参照。重複記述を避けるため本書では再掲しない。

---

## 11. 未確定事項（Step 1 で決まらなかった項目）

| # | 項目 | 暫定方針 | 確定タイミング |
|---|---|---|---|
| TBD-01 | タイマー通知音の種類 | ピン/ベル/チンの候補から v4 実装時に試聴決定 | v4 実装時 |
| ~~TBD-02~~ | ~~プロジェクト数の上限~~ | **最大100件で確定**（v0.4） | ✅ 確定 |
| ~~TBD-03~~ | ~~カウントダウンの上限時間~~ | **MM:SS 形式、99:59 で確定**（v0.4） | ✅ 確定 |
| ~~TBD-04~~ | ~~Todo の最大行数~~ | **1プロジェクト最大1000行で確定**（v0.4） | ✅ 確定 |
| TBD-05 | 競合警告時の「マージ」機能 | **なし、再読込のみで確定** | ✅ Later 候補へ |
| TBD-06 | v1→v2 のデータマイグレーション | AC-DATA-01〜03 でカバー。それ以外は Firestore コンソールで手動補正 | ✅ AC化済み |

---

## 12. 承認チェック

Step 2 ワイヤーフレームへ進む前の承認項目：

- [ ] 画面一覧（S0〜S4 + H）の認識が合っている
- [ ] 主要ユーザーフロー（初回ログイン・日常・プロジェクト切替・ピン留め変更）の認識が合っている
- [ ] 各機能の受け入れ条件（AC-*）に過不足ないと判断できる
- [ ] データ構造（Firestore パス）に同意
- [ ] 計算ルール（日数進捗・休暇残量）に同意
- [ ] 保存仕様（デバウンス・契機・状態UI・競合検知）に同意
- [ ] スマホでの制限事項（インデント変更・行削除不可）に同意
- [ ] 暫定テーマカラーで v1〜v4 を進めることに同意
- [ ] 未確定事項（TBD-01〜05）の扱いに同意

承認後、サブエージェント仕様レビュー → ChatGPT 外部レビュー → 修正反映 → Step 2 ワイヤーフレームへ。
