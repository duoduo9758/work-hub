# BACKLOG.md — work-hub 機能バックログ

> 4分類で管理：**Now**（今回のサイクルで作る）/ **Next**（次サイクルの候補）/ **Later**（いつか）/ **Never**（やらない）
> 思いつき・後から欲しくなった機能はすべてここに登録し、即作業しない。
> 作成日：2026-05-27 / バージョン：**v0.1**

---

## 🟢 Now — 今回の MVP（v1〜v5）に含める

CHARTER.md / SPEC.md で確定したスコープ。詳細は SPEC.md 参照。

### v1
- アクセスコード認証
- 日数進捗トラッカー（CRUD + ピン留め + ヘッダー常時表示）

### v2（v2a〜v2e に分割）
- v2a: プロジェクト別の行メモ（土台）
- v2b: チェックボックス（hasCheckbox トグル + ON/OFF + 取り消し線）
- v2c: PC のインデント変更（Tab/Shift+Tab）
- v2d: スマホ向け最低限の編集UI
- v2e: 保存堅牢化（保存状態UI / 競合検知 / 失敗再試行）

### v3
- 休暇管理（use/adjustment の2種、残量計算、マイナス検知、利用/調整の2タブ履歴、年度フィルタ）

### v4
- タイマー（カウントアップ + カウントダウン、ローカル完結）

### v5
- UI 磨き込み（`frontend-design` 一括適用、モノクロ・ミニマル方針で磨き込み）
- Firestore セキュリティルール本番化、GitHub Pages 公開、v1.0.0 タグ

---

## 🟡 Next — 完成後の最初のサイクル候補

完成（v5）後の最初の機能追加サイクルで取り組む候補。優先度順。

| # | 項目 | 動機 |
|---|---|---|
| N1 | Todo の期限日機能（行ごとに期限日 + 期限切れ/今日期限の視覚表示） | SPEC で外したが、運用してみて欲しくなる可能性高 |
| N2 | タイマーの履歴一覧（簡易版、Firestore 保存） | v4 で外したが、振り返りたくなる可能性 |
| N3 | ダークモード | v0.4 で Never に入れたが、夜の利用で欲しくなる可能性 |
| N4 | 検索機能（Todo 全体検索） | プロジェクト数が増えると探しにくくなる |

---

## 🟠 Later — いつかやる可能性

機能としては意義あるが、しばらくは不要。3個以上たまったら検討。

| # | 項目 | メモ |
|---|---|---|
| L1 | カレンダー連携（Google Calendar / Outlook） | 取得した有給を自動同期等 |
| L2 | 月次／週次レポート（タイマー累積・休暇取得ペース） | 詳細分析。要件が育ったら |
| L3 | ポモドーロ機能（25分作業＋5分休憩の自動切替） | タイマー拡張 |
| L4 | エクスポート（CSV / JSON バックアップ） | 機種変等への対応 |
| L5 | 通知（残休暇少 / 期間終了間際） | プッシュ通知導入が必要 |
| L7 | Todo のドラッグ&ドロップ並び替え | 並び替え運用が定着したら |
| L8 | Todo のタグ・カテゴリ・繰り返しタスク | プロジェクト切替で十分なうちは保留 |
| L9 | Firebase Auth 導入 | 社外秘データを扱う必要が出たら **必須化** |
| L10 | PWA 化（オフライン対応・ホーム画面追加） | スマホ利用率が高まったら |
| L11 | 多言語切替（中国語・英語） | 日本語固定で運用したあとで |
| L12 | 複数ユーザー対応（家族・同僚と共有） | 設計が根本的に変わるため慎重に |
| L13 | Todo保存：transaction失敗時の再fetchで「実は成功」を検知 | **症状**：単一端末で編集中にネット切断 → 保存失敗扱い → 再試行で「他端末で更新されています」モーダルが誤発火する。データロスはなく「再読み込み」で復旧可能だが UX混乱あり。**着手条件**：実害遭遇時 or マルチデバイス利用が増えたら。**実装方針**：`src/features/todo/todo-store.js` の `saveProjectLines` 内で transaction が非CONFLICTエラーで失敗した場合、`loadProject` で再fetch → 自分が送ろうとした内容と一致＋versionが+1されていればサイレント成功として扱う |
| L14 | 休暇保存：多端末同時編集レース窓 | **症状**：端末 A/B が同一 leaveType で同時に追加・編集・削除すると、両方が「残量内」と判定して保存される。結果として残量が想定外（マイナス含む）になる可能性。**原因**：Firebase JS SDK の transaction 制約により、残量計算用の query 読み取りは transaction 外で実施。対象ドキュメントの version-check のみ transaction 内（v0.8.2 SPEC §7-5 実装変更注記）。**現状**：単一端末通常運用では整合性 OK。**着手条件**：複数端末で休暇を同時編集する運用が発生したら、または整合性破綻の実害遭遇時。**実装方針**：Cloud Functions に集約 or 楽観ロック用に leaveType 単位のサマリ doc を導入して transaction 化 |

---

## 🔴 Never — やらない（重要）

「やらない」と明示的に決めたもの。完成までの間に「やりたい」気持ちが出ても押し戻す。

### 機能横断 Never
- ガントチャート / カンバン
- 詳細レポート・統計グラフ（複雑な可視化）
- リアルタイム同期（`onSnapshot` 使用）
- オフライン編集 / 競合解決UI（警告のみ実装）

### Todo 特有 Never
- contentEditable ベースのエディタ
- ステータス（未着手/進行中/完了）フィールド（チェックボックスで代替）
- 優先度（高/中/低）フィールド（行頭に「★」自由記入で代替）
- データ的サブタスク・親子チェック連動
- ファイル添付・コメント
- 表・複数列・画像・絵文字ピッカー
- リッチテキスト書式（太字・色など）
- 複数行をまたぐ範囲選択コピー
- 複数端末・複数タブ同時編集
- 外部連携（Slack / Notion / Gmail）

### 休暇管理 特有 Never
- 残量の直接編集（計算値のみ）
- 年度繰越・自動付与の **自動** 処理（grant/adjustment 手動入力）
- 会社カレンダー反映 / 土日祝除外計算
- 申請ワークフロー / 承認 / 添付 / 証跡
- 人事システム連携
- 半休・時間休（時間単位記録で代替）

### タイマー 特有 Never
- ブラウザを閉じても計測継続
- タブを閉じた瞬間の保存・停止保証
- 複数タイマー同時実行
- タスクとの紐付け
- OS通知 / ブラウザ通知
- スマホロック・バックグラウンド中の正確な計測保証
- 複数端末同期

※ アプリ内 Web Audio による短い通知音は v4 で実装済み（音 ON/OFF はトグルで切替、デフォルト ON）

### 日数進捗トラッカー 特有 Never
- 営業日計算 / 祝日除外
- 時刻単位カウントダウン
- 繰り返し期間（期間終了後の自動更新）
- 複数ステージ / 進捗の手動補正
- カテゴリ色分け（カテゴリ機能ごと不要）
- 複数件の同時ヘッダー表示
- ピン留め対象終了時の自動切替

### 技術面 Never
- Firebase Authentication（Later L9 で再検討対象）
- TypeScript（.jsx 固定）
- E2E テスト・自動テストフレームワーク
- Cloud Functions / CI/CD
- 環境別設定 dev/prod 分離
- エディタライブラリ（Lexical / Slate / TipTap / ProseMirror）

---

## 運用ルール

1. **思いつきで即作業しない**。思いついたらまず本リストに追加
2. **3個以上たまったら**まとめて1サイクル回す
3. Next の項目が増えてきたら、優先度を見直し
4. Later → Never への昇格を定期的に検討（やらないことを決め切る）
5. Now の項目は完成まで動かさない（途中で要件追加は厳禁）

詳細は WORKFLOW.md「完成後の機能追加」を参照。
