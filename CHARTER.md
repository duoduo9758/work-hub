# CHARTER.md — work-hub プロジェクト憲章

> 本ドキュメントは「やらないこと」を先に決めるための合意書。
> 仕様凍結（SPEC.md）の前段階で、スコープ・成功条件・除外範囲を確定させる。
> 作成日：2026-05-28 / バージョン：**v0.11**（v2b-extra として Todo の Undo/Redo を追加。構造変更のみが対象、メモリ内のみ、スマホはサポート外）

---

## 0. 改訂履歴

| Ver | 主な変更 |
|---|---|
| v0.1 | 初版（4機能 / Firestore同期 / カスタム認証） |
| v0.2 | ChatGPT外部レビュー反映：v1〜v5段階、認証現実明記、再読み込み同期、休暇1h単位 |
| v0.3 | Todo を OneNote 風エディタに変更、進捗バーヘッダー常時表示、タイマー再設計、休暇手動編集 |
| v0.4 | ChatGPT v0.3 再レビュー反映：**Todoを行コンポーネント方式に確定**、**v2をv2a〜v2eに分割**、**期限日フィールド削除**、**保存堅牢化**、**休暇調整レコード方式**、**タイマー次回開時停止扱い** |
| v0.5 | ユーザー意見反映：**デザイン方針＝モノクロ・ミニマルを最初から固定**、**ボタン3階層濃淡**、**休暇は「取得・調整」2種に統合（付与廃止）**、**休暇履歴を利用/調整の2タブ表示**、**タイマー通知音追加（ON/OFF設定）**、**進捗バーに%表示追加** |
| v0.6 | サブエージェント仕様レビュー反映：**Todoエディタ細部確定（行分割Enter / 行結合Backspace / textareaベース / PC複数行ペースト分割）**、**休暇use上限1日8h・重複可・調整-9999〜+9999h**、**タイマーStart/Stop文脈切替・カウントダウン中編集即時反映**、**機密入力注意書き撤廃**、**IME抑制 / 保存ロック / 競合検知のtransaction化** など |
| v0.7〜v0.8 | ChatGPT DATA.mdレビュー反映：文書矛盾統一・行id復活・Todo上限と容量チェック・Security Rules強化・アクセスコード8文字以上必須 など |
| **v0.9** | **休暇仕様の根本改訂**：**use を「X日Y時間」2フィールド入力に**（長期休暇を1レコードで対応）、**有給年度を9/1〜翌8/31に変更**（会社固有ルール）、**丸1日有給5日必須警告ロジック追加** |
| **v0.11** | **Todo の Undo/Redo を v2b-extra として追加**：構造変更のみ対象、履歴上限50、プロジェクト切替/conflict reloadでクリア、スマホUIは非対応 |

### v0.4 → v0.5 改訂サマリ

| 項目 | v0.4 | v0.5 |
|---|---|---|
| デザイン方針 | v1〜v4 装飾なし、v5 で一括適用 | **モノクロ・ミニマルを最初から固定**、v5 で磨き込み |
| 暫定テーマカラー | ウォームオレンジ系 | **モノクロ系（白・黒・グレー基調、ChatGPT/Apple風）** |
| ボタンの濃淡 | 言及なし | **3階層（プライマリ黒/セカンダリ薄灰/ターシャリ透明）を厳格に使い分け** |
| 休暇のレコード種類 | grant / use / adjustment の3種 | **use / adjustment の2種に統合**（付与は調整のnote「年度付与」で表現） |
| 休暇画面のボタン | +付与 / +取得 / +調整 の3つ | **+取得（プライマリ）/ +調整（セカンダリ）の2つ** |
| 休暇履歴の表示 | 1リストに全種混在 | **2タブ：「利用履歴」（メイン）/「調整履歴」、年度フィルタ付き** |
| タイマー通知音 | 音は出さない | **デフォルトON、設定で OFF 可、短い1音** |
| 進捗バーの表示 | タイトル / バー / 残N日 | **+ 進捗パーセント（例：「47%」）** |

### v0.3 → v0.4 改訂サマリ

| 項目 | v0.3 | v0.4 |
|---|---|---|
| Todo の実装方式 | contentEditable で OneNote風 | **行コンポーネント方式（input/textarea ベース、各行独立）** |
| Todo の呼称 | OneNote 風エディタ | **チェック付きアウトラインメモ** |
| v2 の規模 | 一気にすべて | **v2a〜v2e に5段階分割** |
| Todo 期限日 | 行ごとに任意で入力 | **フィールドなし、行テキストに自分で「（〜5/30）」と書く** |
| Todo デバウンス | 5秒 | **1.5秒、チェックボックスは500ms以内** |
| 保存契機 | デバウンス + beforeunload | **+ visibilitychange / pagehide / プロジェクト切替時**。beforeunloadは未保存警告のみ |
| 「編集ロスゼロ」表現 | あった | **削除**（保証できない） |
| Todo の競合検知 | なし | **version フィールド、サーバー側不一致なら警告** |
| 保存状態UI | なし | **保存中/保存済み/未保存/保存失敗を画面表示** |
| 休暇の残量直接編集 | 可能 | **禁止**。残量は計算値のみ |
| 休暇の年度切替・追加付与 | 残量を手動編集 | **調整レコード方式（grant/use/adjustment）** |
| 休暇のマイナス検知 | 新規登録時のみ | **編集・削除時にも再計算してチェック** |
| タイマーの計測方式 | setInterval カウント | **Date.now() 差分計算** |
| タイマーのタブ閉じ停止 | beforeunloadで停止 | **次回開いた時に停止扱いで復帰**（保証しない方式） |
| 進捗バー終了後 | 100% 固定で表示し続ける | **「終了」表示＋ピン解除/変更ボタン**（自動切替しない） |
| スマホのヘッダーバー | 40〜50px | **1行圧縮表示**（タイトル / 残N日 / 細いバー） |
| スマホ Todo 操作 | 未定義 | **閲覧 + チェックON/OFF + 行追加 + テキスト編集のみ**。インデント・行削除は PC 限定 |

---

## 1. このアプリは誰のためのものか

- **対象ユーザー**：開発者本人（1人専用）
- **想定利用環境**：
  - 会社PC（Windows / Chrome）でメイン利用
  - 自宅PC・スマホからも参照・軽い操作
  - **同一アカウントで複数端末同期が必須要件**
- **想定スキル**：プログラミング初心者。Claude Code に実装を任せる前提

---

## 2. 何の課題を解決するのか

| 散らばっている現状 | 解決後 |
|---|---|
| プロジェクトごとのTodoが分散、OneNote は重い | プロジェクト単位の軽量「チェック付きアウトラインメモ」に集約 |
| 有給・傷病休暇の残量を毎回人事システムで確認 | アプリ内で常に見える（X日Y時間表示、整合性のある計算値） |
| 作業時間を意識せずダラダラ作業してしまう | 今のセッションの経過を可視化（記録・分析はしない） |
| 「このプロジェクトあと何日？」を都度計算 | ヘッダー固定の進捗バーで一目で分かる |

**コアバリュー**：アプリを開けば、今やってるプロジェクトの残り日数が画面上に常に見え、その下でタスクをチェック消化していけて、休暇残量も簡単に分かる。

---

## 3. MVP の段階分け

### v1: 土台 + 日数進捗トラッカー
- プロジェクト初期化、Firestore接続、アクセスコードログイン
- 日数進捗トラッカー（一覧画面 + ヘッダー固定進捗バー）
- レスポンシブの基本（PC + スマホ375px）
- **v1 ではデザインを盛らない**。細いバーと数字だけ
- commit: v1 baseline

### v2: Todo — **5段階に分割**

**v2 全体は単独プロジェクト級の規模であることを前提に、サブフェーズで段階的に積み上げる**。各サブフェーズ完了で commit + 動作確認。

#### v2a: プロジェクト別の行メモ（土台）
- プロジェクトの追加・選択・削除・名前変更
- 行の追加・編集・削除（**行コンポーネント方式、各行input/textarea**）
- Enter で下に新規行追加、Backspace（空行）で行削除
- Firestore 保存（**1.5秒デバウンス + visibilitychange/pagehide で保存**）
- **保存状態UI**（保存中/保存済み/未保存/保存失敗）
- **競合検知（conflict detection）**（version フィールド、不一致なら警告）
- チェックボックスなし、インデントなし、期限なし
- commit: v2a base

#### v2b: チェックボックス
- 行ごとのチェックボックス有無のトグル（Ctrl+1 / 行頭ボタン）
- ON ↔ OFF のクリックトグル
- チェック済み = 取り消し線＋薄色
- チェックボックス操作は **500ms以内に保存**
- 親子チェック連動なし（各行独立）
- commit: v2b checkbox

#### v2c: インデント（PC のみ）
- Tab / Shift+Tab で行のインデントレベルを変更（**PC 限定**）
- スマホではインデント変更はできない（閲覧時はインデント表示は反映される）
- 見た目のみ、データ的な親子関係は持たない
- commit: v2c indent

#### v2d: スマホ向け最低限の編集UI
**前提：スマホは閲覧メイン、実編集はほぼPCで行う**
- スマホで可能な操作：**閲覧 / チェックボックスON/OFF / 行の追加 / テキスト編集**
- スマホで不可の操作：**インデント変更 / 行削除**（PC限定）
- スマホでは各行に表示する操作ボタンを **チェックボックストグルのみ**に絞る
- 行追加はソフトキーボードの改行キー or 末尾の「+ 新規行」ボタン
- PC では Tab / Shift+Tab でインデント、Backspace（空行）で削除
- commit: v2d mobile minimal

#### v2e: 保存堅牢化と仕上げ
- 保存失敗時の警告表示・再試行
- 競合時の「他端末で更新されています。再読み込みしてください」表示
- プロジェクト切替時の強制保存
- 動作確認・統合テスト
- commit: v2 complete

### v3: 休暇管理
- 有給・傷病休暇の残量管理（X日Y時間表示）
- **use（取得）/ adjustment（調整）** の2種類のレコードを保存
- **use 入力は「X日 Y時間」の2フィールド**、adjustment 入力は「正負トグル + X日 + Y時間」の3フィールド
- **残量は計算値のみ**（直接編集不可）
- 残量計算式：`Σ(adjustment.hours) − Σ(use の合計時間 = days*8 + hours)`
- 編集・削除時にも残量再計算、マイナスになる操作はエラー
- 1日=8h（換算用）、入力は整数のみ
- 有給年度 9/1〜翌8/31、丸1日有給5日必須警告（有給のみ）
- 2軸表示：残量 / 今年度取得済み
- commit: v3 leave added

### v4: タイマー
- カウントアップモード / カウントダウンモード切替
- **Date.now() 差分計算**（setInterval カウントしない）
- Start / Stop / Reset
- カウントダウン到達で自動停止＋画面内視覚通知（赤系、テキスト、タブタイトル変更）
- **タブを閉じても保証しない**：次回開いた時、実行中フラグが残っていたら「停止状態として復帰」
- 履歴なし、Firestore なし、ローカル完結
- カウントダウンのデフォルト時間のみ localStorage
- commit: v4 timer added

### v5: UI 磨き込み
- `frontend-design` スキル一括適用
- commit: v5 ui polished

---

## 4. 各機能の MVP 仕様

### 4-1. 日数進捗トラッカー（v1）

**MVP仕様：**
- 入力項目：タイトル / 開始日 / 終了日 の3つのみ
- 日数計算：日本時間のカレンダー日
- 開始日 = 0%、終了日 = 100%
- 終了後：100% 固定で **「終了」表示**
- 最大10件
- 並び順：終了日昇順
- 1件を「ピン留め」可能。ピン留めしたものをヘッダー固定表示
  - 表示：**タイトル + 進捗バー + 残N日/全N日 + 進捗パーセント（例：47%）**
  - **ピン留め中の期間が終了済みになったら**：「終了」表示＋ピン解除/変更ボタン（**自動切替しない**）
  - ピン解除はヘッダー上から1クリック可能（プレッシャー逃げ道）
- **スマホでは1行圧縮表示**（タイトル / 残N日 / % / 細いバー）。詳細はタップ展開

**やらないこと：**
- 営業日計算 / 祝日除外
- 時刻単位カウントダウン
- 繰り返し期間 / 複数ステージ
- 進捗の手動補正 / カテゴリ色分け
- 複数件の同時ヘッダー表示
- 終了後の自動切替

### 4-2. Todo（v2）— 「チェック付きアウトラインメモ」

**実装方式（最重要・確定）：**
- **行コンポーネント方式**。`contentEditable` は使わない
- 各行は独立した `input` または `textarea` ベース
- データ構造：1プロジェクト = 1Firestore ドキュメント、行配列を保存

**各行が持つフィールド：**
- テキスト（自由入力）
- チェックボックス有無（trueなら表示）
- チェック状態（ON / OFF）
- インデントレベル（0〜数階層、見た目のみ）
- ※期限日フィールドは作らない（行テキスト内に「（〜5/30）」など自分で書く）

**操作（PC）：**
- Enter：新規行を下に追加
- Tab / Shift+Tab：インデント変更
- Ctrl+1：チェックボックス挿入 / 削除トグル
- チェックボックスクリック：ON ↔ OFF トグル
- 行頭で Backspace（行が空）：行削除

**操作（スマホ）— 閲覧メイン前提：**
- スマホでできること：**閲覧 / チェックボックスON/OFF / 行追加 / テキスト編集**
- スマホでできないこと：**インデント変更 / 行削除**（PC限定）
- 行追加はソフトキーボードの改行 or 末尾の「+ 新規行」ボタン
- 操作ボタンは **チェックボックスのみ** 表示（インデント±や削除ボタンは出さない）

**チェック済みの表示：**
- 取り消し線＋文字色を薄く

**親子チェック連動なし：**
- Tab段下げは見た目のみ
- 各行独立、親をチェックしても子は変わらない

**保存：**
- **1.5秒デバウンス** で Firestore に書き込み
- **チェックボックス操作は500ms以内に保存**
- `visibilitychange` / `pagehide` でも保存試行
- プロジェクト切替時に強制保存
- `beforeunload` は **未保存時の警告表示のみ**（保存保証には使わない）
- **編集ロスゼロは保証しない**（モバイル・通信断・強制終了時は消える可能性）

**保存状態UI：**
- エディタ上部 or 右上に状態表示：
  - 「保存済み HH:MM」
  - 「保存中...」
  - 「未保存の変更あり」
  - 「保存失敗。再試行してください」+ 再試行ボタン

**競合検知：**
- 各プロジェクトドキュメントに `version`, `updatedAt`, `clientId` を持たせる
- 保存時にサーバー上の version が読み込み時 version と異なる場合 → **自動上書きせず警告**
- 「他端末で更新されています。再読み込みしてください」と表示
- 複数端末・複数タブでの同時編集はサポートしない

**やらないこと：**
- ステータス（未着手/進行中/完了）→ チェックボックスで代替
- 優先度（高/中/低）→ 自分で行頭に「★」など書けばよい
- **期限日フィールド・期限切れアラート・通知**
- 完了タスクのアーカイブ・自動非表示
- 編集履歴の永続化（直近の Undo/Redo は v2b-extra で実装。リロード後は消える）
- データ的なサブタスク・親子チェック連動
- ファイル添付・コメント・タグ・繰り返し
- ドラッグ&ドロップ並び替え
- 外部連携（Slack / Notion / Gmail）
- 検索・フィルタ
- 表・複数列・画像・絵文字ピッカー
- リッチテキスト書式
- 複数行をまたぐ範囲選択コピー
- 複数行の同時編集

### 4-3. 休暇管理（v3）

**データ構造（v0.9 改訂：use を X日Y時間 2フィールドに）：**

2種類のレコードを「履歴」として保存し、残量は計算値のみ：

```js
// use レコード（取得）
{
  type: "use",
  leaveType: "paid" | "sick",
  days: integer ≥ 0,         // X日部分（丸1日カウントの基準）
  hours: integer 0〜7,        // Y時間部分（半端時間）
  date: "2026-05-27",
  note: "午前" など,           // 任意（30文字まで）
}

// adjustment レコード（調整）
{
  type: "adjustment",
  leaveType: "paid" | "sick",
  hours: integer (-9999〜+9999、正負可),  // days フィールドは持たない
  date: "2026-05-27",
  note: "年度付与" など,
}
```

- **use**：取得記録（例：5/15 に 0日4時間、または 3日0時間、または 3日4時間）。**長期休暇も1レコードで入力可能**
- **adjustment**：調整（年度付与・繰越・誤計上修正など）。`hours` のみで日数フィールドなし

「付与」は撤廃。初期付与は「note=年度付与」の adjustment レコードとして登録する運用。

**残量計算（常に再計算）：**
```
残量 = Σ(adjustment.hours) − Σ(use の合計時間)
　ただし use の合計時間 = days * 8 + hours
```

**操作：**
- use / adjustment レコードの追加・編集・削除が可能
- **残量を直接編集することはできない**
- 各操作の保存前に残量再計算 → **マイナスになる操作はエラーで保存不可**
- 残量・取得済み・履歴のすべての表示は「X日 Y時間」形式に統一（**1日=8h固定の換算用**）。h 単独表記は使わない
- 入力は **整数のみ**（小数禁止）

**会社固有ルール（v0.9 追加）：**
- **有給年度は 9/1〜翌8/31**（会計年度4/1〜3/31とは別）
- **年5日以上の「丸1日有給」取得が必須**
  - 「丸1日」= use レコードの `days` フィールドが基準（半端時間 hours は含めない）
  - 集計：現在の有給年度内の paid use レコードの `days` の合計
- 履歴の年度フィルタも 9/1〜翌8/31 で統一

**休暇画面のサマリ表示（v0.9 簡略化）：**

初回利用時の問題（取得履歴がないと丸1日5日達成かわからない）を解決するため、**2軸表示** を採用：

```
【有給休暇】
残量：19日 4時間
今年度取得済み：0日 4時間
丸1日取得：0 / 5日（未達、会社ルール目安）

【傷病休暇】
残量：4日 0時間
今年度取得済み：1日 0時間

※ 今年度 = 2026/9/1〜2027/8/31（会社有給年度、会計年度4/1とは別）
※ すべて「X日 Y時間」形式で統一表示（h 単独表記なし）
```

**5日警告の表示ルール（v0.10 改訂：認知負荷低減）：**
- 「今年度取得済み」とは **別行に** 「丸1日取得：◯ / 5日（未達 or 達成）」を表示
- 5日未達：warning スタイル、「丸1日取得：0 / 5日（未達、会社ルール目安）」
- 5日達成：通常スタイル、「丸1日取得：5 / 5日（達成）」← 達成状態も見せる
- 傷病カードには「丸1日取得」行を表示しない
- 警告対象は **有給のみ**、傷病には表示しない
- 過去分を入れる/入れないはユーザー自由。「今年度取得済み」を見て自己判断

**画面構成（v0.5：2タブ）：**
- 上部：残量サマリ（有給 / 傷病、それぞれ「残h（◯日◯h）」）
- 操作ボタン：**[+ 取得]（プライマリ・濃い）** / **[+ 調整]（セカンダリ・薄い）**
- 履歴エリア：**2タブ切替**
  - **「利用履歴」タブ（メイン・デフォルト）**：use レコードのみ、日付降順
  - **「調整履歴」タブ**：adjustment レコードのみ、日付降順
- 年度フィルタ（「現在年度」「過去年度」「すべて」）：履歴を絞り込み

**やらないこと：**
- 残量の直接編集
- 年度繰越・自動付与の **自動** 処理（手動で adjustment を入れる運用）
- 会社カレンダー反映 / 土日祝除外
- 申請ワークフロー / 承認 / 添付 / 証跡
- 人事システム連携

### 4-4. タイマー（v4）

**MVP仕様：**

**2モード：**
- **カウントアップモード**：0:00 から無限にカウントアップ
- **カウントダウンモード**：指定時間（例：25:00）から 0:00 へ

**計測方式：**
- **`Date.now()` の差分計算**（`setInterval` カウントしない）
- 開始時刻を保存しておき、現在時刻 - 開始時刻 で経過秒を算出
- バックグラウンドからの復帰でズレない

**操作：**
- Start / Stop / Reset の3ボタンのみ
- カウントダウン到達で **自動停止**

**カウントダウン到達時の通知（v0.5：音追加）：**
- タイマー表示を **赤系** に変更
- 画面内に「時間です」表示
- **タブタイトル**に完了マーク（`⏰ 時間です | work-hub` など）
- **短い通知音を1回再生**（Web Audio API でブラウザ内再生、OS通知やブラウザ通知ではない）
- **音の ON/OFF は設定で切替可能、デフォルト ON**
- 音の種類は v4 実装時に試聴して決定（候補：シンプルなビープ／ベル／チン）
- 画面非表示中（タブ切替中・スマホロック中）の通知到達は保証しない
- 仕事中の利用を想定し、過度に大きい音・長い音は避ける

**タブ閉じ時の挙動：**
- **「閉じた瞬間に停止」を保証しない**
- 実行中フラグを localStorage に持つ
- **次回開いた時、実行中フラグが残っていたら「停止状態として復帰」**（自動で経過時間を保存・継続はしない）
- スマホでブラウザがバックグラウンドに行った場合の正確性は保証しない

**保存：**
- **履歴は保存しない**
- **Firestore への書き込みなし**（端末ローカル完結）
- カウントダウンの「最後に使った時間」のみ localStorage（業務データではない）

**コアバリューの再定義：**
> 「作業時間を意識化」ではなく **「今このセッションの経過を可視化する」**。
> 記録・分析・集計はしない。タイマーは現セッション専用の表示装置。

**やらないこと：**
- 履歴一覧・累積集計
- ブラウザを閉じても計測継続
- 複数タイマー同時実行
- タスクとの紐付け
- OS通知 / ブラウザ通知（v0.5 で内部音は許可、OS/ブラウザ通知は不可）
- スマホロック中の正確な計測保証
- 複数端末同期

---

## 5. 同期・データ方針

### 5-1. 同期レベル
- **再読み込み時同期のみ**
- 画面ロード時 / プロジェクト切替時 / タブ切替時に Firestore から最新取得
- `onSnapshot` は使わない

### 5-2. データ保存方針
- **Firestore は業務データ（Todo / 日数トラッカー / 休暇）の永続化先**
- **タイマーは Firestore に保存しない**
- localStorage に保存してよいもの：アクセスコード、UI設定、カウントダウンのデフォルト時間、タイマー実行中フラグ
- localStorage に業務データは保存しない

### 5-3. Todo 編集の保存方針（v2 詳細）
- **デバウンス1.5秒**（最後の入力から1.5秒経過で保存）
- **チェックボックス操作は500ms以内に保存**
- 以下の契機でも保存試行：
  - `visibilitychange`（タブが非表示になった瞬間）
  - `pagehide`（ページ離脱の瞬間）
  - プロジェクト切替時
- `beforeunload` は **未保存時の警告表示のみ**（保存保証には使わない）
- **編集ロスゼロは保証しない**

### 5-4. 競合検知（Todo 専用）
- 各プロジェクトドキュメントに `version`, `updatedAt`, `clientId` を保持
- 保存時にサーバー側 version が読み込み時と異なる場合：
  - 自動上書きせず警告表示
  - 「他端末で更新されています。再読み込みしてください」
- 複数端末・複数タブ同時編集はサポート外

---

## 6. 認証・セキュリティ方針

### 6-1. 認証方式
- カスタム文字列ID（アクセスコード）方式
- 形式：英数字・アンダースコア・ハイフン、**8〜64文字**（v0.8 改訂、推奨 12文字以上）
- Firebase Auth は **使わない**

### 6-2. セキュリティの現実
- カスタム文字列ID方式は **簡易保護**、強固な認証ではない
- **本MVPでは社外秘・機密情報・個人情報・顧客名・案件実名を一切入力しない**
- Todo の行内容は「資料作成」「レビュー依頼」など抽象表現
- プロジェクト名は「案件A」「100日チャレンジ」など匿名化
- 高機密データを扱う必要が出たら Firebase Auth 導入を再定義
- ユーザー本人の自己管理ルールとして社外秘は入れない（画面上の注意書きは出さない、v0.6 でユーザー判断により撤廃）

### 6-3. Firestore セキュリティルール
- テストモード（30日期限）のまま放置しない
- 使用パスのみ `allow read, write: if true`
- 公開前（Step 10）に本番ルールへ切り替え

---

## 7. 利用環境

| 項目 | 内容 |
|---|---|
| デプロイ先 | GitHub Pages（`duoduo9758/work-hub`） |
| バックエンド | Firebase Firestore（**Spark無料プラン厳守**） |
| 認証 | カスタム文字列ID（簡易） |
| メインブラウザ | Chrome（PC・スマホ） |
| 対応幅 | PC：1280px〜 / スマホ：375px〜414px |
| 言語 | **日本語固定** |
| 費用 | **完全無料**。Firestore 1日2万読み・2万書き以内 |

---

## 8. 成功条件

### v1 完成条件
- [ ] 日数進捗トラッカーの CRUD が動く
- [ ] ピン留めしたトラッカーがヘッダーに常時表示される（スマホは1行圧縮）
- [ ] アクセスコードログインで自分のデータが読み書きできる
- [ ] PC・スマホ両方で表示崩れがない
- [ ] 会社PCで作成 → 自宅PCで再読み込み → 反映される
- [ ] コンソールに `error` / `warn` が出ない
- [ ] GitHub Pages で公開済み
- [ ] **v1 ではデザインを盛らない**（細いバーと数字のみ）

### v2 完成条件（5サブフェーズすべて達成）

**v2a:**
- [ ] プロジェクト追加・選択・削除・名前変更ができる
- [ ] 行の追加・編集・削除ができる
- [ ] 保存状態UI（保存中/保存済み/未保存/失敗）が表示される
- [ ] 競合検知でサーバー側変更を検知できる

**v2b:**
- [ ] チェックボックスのトグルが効く
- [ ] チェック済みが取り消し線で表示される
- [ ] チェック操作が500ms以内に保存される

**v2c:**
- [ ] Tab/Shift+Tab でインデント変更（PC）
- [ ] インデントレベルが見た目に反映される

**v2d:**
- [ ] スマホで閲覧・チェックボックスON/OFF・行追加・テキスト編集ができる
- [ ] スマホではインデント変更・行削除のボタンは出ない（PC限定でOK）

**v2e:**
- [ ] 保存失敗時の警告と再試行ボタンが動く
- [ ] 競合警告メッセージが出る
- [ ] プロジェクト切替時に保存される
- [ ] v1 機能を壊していない

### v3 完成条件（休暇追加）
- [ ] use レコードを「X日 Y時間」2フィールドで追加・編集・削除できる
- [ ] adjustment レコードを「Z時間（正負）」で追加・編集・削除できる
- [ ] 残量が常に計算値として表示される
- [ ] マイナスになる操作はエラーで止まる
- [ ] 残量・今年度取得済み・履歴の表示が**すべて「X日 Y時間」形式に統一**（h単位表記なし）
- [ ] 履歴が「利用履歴」「調整履歴」の2タブに分かれる
- [ ] 年度フィルタ（9/1〜翌8/31）で履歴を絞り込める
- [ ] 「取得」ボタンが濃く、「調整」ボタンが薄く表示される
- [ ] 丸1日有給が5日未満なら警告表示、5日以上で警告消滅
- [ ] v1/v2 を壊していない

### v4 完成条件（タイマー追加）
- [ ] カウントアップ / カウントダウンの切替ができる
- [ ] Date.now() 差分計算で表示される
- [ ] Start / Stop / Reset が正しく動く
- [ ] カウントダウン到達で視覚通知（赤系、テキスト、タブタイトル）+ 音通知（短い1音）
- [ ] 音の ON/OFF を設定で切り替えられる
- [ ] 次回開いた時、実行中フラグが残っていたら停止状態として復帰
- [ ] v1〜v3 を壊していない

### v5 完成条件
- [ ] `frontend-design` 適用後、UIが「毎朝開きたくなる」品質
- [ ] Firestore セキュリティルールが本番モード
- [ ] README に最低限の説明あり

---

## 9. やらないこと（最終版）

### 機能横断 Never
- チーム共有・コラボ
- カレンダー連携（Google Calendar / Outlook）
- 通知・リマインダー（プッシュ / メール / ブラウザ通知）
  ※ タイマー到達時のアプリ内短音のみ例外として許可（CHARTER 4-4 参照）
- 検索 / 高度なフィルタ
- 詳細レポート・統計グラフ
- エクスポート / インポート
- 多言語切替（日本語固定）
- **ダークモード**
- ガントチャート / カンバン

### Todo 特有 Never
- contentEditable ベースのエディタ
- ステータス・優先度フィールド（チェックボックスで代替）
- **期限日フィールド・期限切れアラート**（行テキストに自分で書く運用）
- 完了タスクのアーカイブ・自動非表示
- 編集履歴の永続化（直近の Undo/Redo は v2b-extra で実装。リロード後は消える）
- データ的サブタスク・親子チェック連動
- ファイル添付・コメント・タグ・繰り返し
- ドラッグ&ドロップ並び替え
- 外部連携
- 検索
- 表・複数列・画像・絵文字ピッカー
- リッチテキスト書式
- 複数行をまたぐ範囲選択コピー
- 複数端末・複数タブ同時編集

### 休暇管理 特有 Never
- 残量の直接編集（計算値のみ）
- 年度繰越・自動付与の **自動** 処理（手動 adjustment レコードで運用）
- 「付与」レコード種別の独立管理（v0.5 で「調整」に統合済み）
- 会社カレンダー反映 / 土日祝除外
- 申請ワークフロー / 承認 / 添付
- 人事システム連携

### タイマー 特有 Never
- 履歴一覧・累積集計
- **ブラウザを閉じても計測継続**
- タブを閉じた瞬間の保存・停止保証
- 複数タイマー同時実行
- タスクとの紐付け
- OS通知 / ブラウザ通知（内部音は v0.5 で許可、ON/OFF切替可）
- スマホロック・バックグラウンド中の正確な計測保証
- 複数端末同期

### 日数進捗トラッカー 特有 Never
- 営業日計算 / 祝日除外
- 時刻単位カウントダウン
- **繰り返し期間**
- 複数ステージ / 進捗手動補正
- カテゴリ色分け
- 複数件同時ヘッダー表示
- 終了時の自動切替

### 技術面 Never
- Firebase Authentication
- PWA化
- 自動テスト
- TypeScript
- onSnapshot リアルタイム同期
- Firestore オフライン編集 / 競合解決UI（警告のみ）
- 楽観的UI更新
- Cloud Functions / CI/CD
- 環境別設定 dev/prod 分離
- エディタライブラリ（Lexical/Slate/TipTap など）

---

## 10. Later（完成後検討）

- カレンダー連携
- 月次/週次レポート
- ポモドーロ機能
- Todo の期限日機能（v2 で外したが将来検討）
- タスクのタグ・繰り返し
- 検索機能
- エクスポート（CSV / バックアップ）
- 通知
- Firebase Auth 導入（社外秘扱う必要時）
- Todo のドラッグ並び替え（Undo/Redo は v2b-extra で実装済み）
- ダークモード
- タイマー履歴（v4 で外したが将来検討）

---

## 11. リスクと事前に意識すること

| リスク | 対策 |
|---|---|
| v2 が壁になる | **v2a〜v2e に分割**、各サブフェーズで commit + 動作確認、いつでも公開可能な状態を維持 |
| Todo 編集中の頻繁な書き込みで Firestore 無料枠超過 | デバウンス1.5秒、開発中に課金ダッシュボード確認 |
| beforeunload の不確実性 | visibilitychange/pagehide で先に保存試行、保存状態UI で気づける |
| 休暇の残量整合性崩壊 | 残量直接編集禁止、調整レコード方式で履歴に残す |
| 1時間単位休暇のマイナス | 編集・削除時にも残量再計算チェック |
| スマホでの Todo 操作 | スマホは閲覧+チェック+行追加+テキスト編集のみ。インデント・削除は PC 限定で実装軽量化 |
| ヘッダーバーがスマホで圧迫 | 1行圧縮表示、詳細はタップ展開 |
| 進捗バーの心理的プレッシャー | ピン解除をワンタップで可能に（逃げ道設計） |
| 認証の弱さを忘れて社外秘を入れる | ユーザー本人の自己管理ルール（注意書きは画面に出さない）。リスク受容済み |
| UI 品質を最重要視 → デザインで時間を使いすぎる | **v1 から モノクロ・ミニマル方針を CSS変数で固定**、装飾は増やさない。v5 で `frontend-design` 磨き込み |
| デザイン方針が途中でブレる | テーマカラーと余白・角丸・フォントを v1 で確定、v5 まで変更禁止 |
| ボタンの濃淡が機能ごとにバラバラ | プライマリ/セカンダリ/ターシャリの3階層を全画面で統一（取得=プライマリ、調整=セカンダリ等） |

---

## 12. 承認チェック

以下に同意した上で Step 1 へ進む：

- [ ] v1 / v2(a〜e) / v3 / v4 / v5 の段階リリース計画
- [ ] 実装順（日数トラッカー → Todo → 休暇 → タイマー → UI）
- [ ] **Todo は行コンポーネント方式（contentEditable 禁止）**
- [ ] **Todo の期限日フィールドは作らない**（行テキスト内に自分で書く）
- [ ] **休暇は残量直接編集禁止、use/adjustment の2種レコード方式、履歴は利用/調整の2タブ表示**
- [ ] **タイマーはタブ閉じ停止を保証しない、次回開時に停止扱いで復帰、通知音は ON/OFF 切替可（デフォルト ON）**
- [ ] **デザイン方針：モノクロ・ミニマル（ChatGPT/Apple系）を v1 から固定、v5 で磨き込み**
- [ ] **ボタン濃淡 3階層（プライマリ/セカンダリ/ターシャリ）を全画面で統一**
- [ ] **進捗バーにパーセント表示も追加**
- [ ] 進捗バーのピン留め終了後は「終了」表示＋ピン解除/変更ボタン（自動切替しない）
- [ ] **スマホは閲覧メイン**：Todo は閲覧+チェック+行追加+テキスト編集のみ可能、インデント・削除は PC 限定。ヘッダーバーは1行圧縮表示
- [ ] 保存方針：1.5秒デバウンス + visibilitychange/pagehide + 保存状態UI + 競合検知
- [ ] **編集ロスゼロは保証しない**（モバイル・通信断・強制終了時は消える可能性あり）
- [ ] 社外秘・機密情報を入れない前提
- [ ] 「やらないこと」最終版に同意

承認後、Step 1（SPEC.md）へ進む。
